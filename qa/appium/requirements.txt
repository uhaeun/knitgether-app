# Appium UI 스위트 의존성.
# 2026-08-30에 실행 중이던 가상환경에서 뽑았다. 이 파일이 없어서 다른 경로에
# 환경을 다시 만들 수 없었고, 그 사실이 프로젝트 이동 중에 드러났다.
# 직접 쓰는 것은 앞의 넷이고 나머지는 그 의존성이다.
Appium-Python-Client==6.0.0
pytest==9.1.1
pillow==12.1.0
requests==2.33.0
