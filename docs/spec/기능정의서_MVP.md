# 기능정의서 MVP (화면 단위)

기획 명세(docs/spec/원본_v2.2.md)와 개발 과정에서 확정된 동작을 화면 단위로 정리한 문서다. 동작 기술은 현재 코드(main) 기준이며 QA 산출물(02, 05, 09, 10)과 대조했다.

## 범위

포트폴리오 QA가 다룬 MVP 기능만 다루며, TC 그룹(09 문서 1절 TC1~TC16)과 영역을 일치시킨다. 온보딩, 회원가입, 로그인, 로그아웃, 프로젝트 목록(생성, 수정, 삭제, 정렬과 필터, 즐겨찾기, 개수 한도), 작업 공간(도안 영역, 카운터, 정보 탭), 재방문 보존, 창고(실, 바늘, 도구 등록과 수정과 삭제), 오프라인 생성 후 동기화가 대상이다. 그 밖의 기능은 마지막의 범위 외 절에 이름만 적는다.

표의 요소 열은 코드의 접근성 ID(AppAccessibilityID.swift) 또는 화면의 실제 라벨을 쓴다. 예외 처리 열은 코드에서 확인된 것만 적고, 코드에 해당 처리가 없으면 정의 없음이라고 적는다. 정의 없음 항목은 마지막 절에 모아 정리한다. 기대 결과의 SPEC ID는 02_기능 동작 정의서의 동작 ID를 가리킨다.

---

## 온보딩 (Views/Onboarding/OnboardingView.swift)

여섯 페이지로 구성된다. 순서대로 도입, 계정, 프로필, 단위, 스킬 테스트, 준비 완료다. 상단 점 표시가 현재 페이지를 나타낸다.

| 요소 | 동작 | 기대 결과 | 예외 처리 |
| --- | --- | --- | --- |
| 다음 버튼 (onboarding.start) | 탭 | 다음 페이지로 이동한다 | 저장 중에는 비활성화된다 |
| 이전 버튼 (라벨 "이전") | 탭 | 이전 페이지로 이동한다. 첫 페이지에서는 표시되지 않는다 | 해당 없음 |
| 계정 만들기 / 로그인 (onboarding.login) | 탭 | 계정 화면(AuthAccountView)으로 이동한다. 로그인하지 않고 다음으로 건너뛸 수 있다 | 해당 없음 |
| 이름 필드 (프로필 페이지) | 입력 | 앱에서 쓸 표시 이름을 받는다 | 공백 제거 후 비어 있으면 기본값 "뜨개러"로 저장한다 |
| 단위 세그먼트 (단위 페이지) | 선택 | Metric과 US 중 하나를 저장한다 | 해당 없음 |
| 스킬 테스트 시작 (onboarding.start) | 탭 | 스킬 테스트 화면으로 이동한다. 테스트 자체는 범위 외다 | 해당 없음 |
| 시작 버튼 (onboarding.complete) | 탭 | 프로필을 저장하고 온보딩 완료 플래그를 기록한 뒤 메인 탭으로 진입한다 | 저장 실패 시 "시작 정보를 저장하지 못했어요."를 마지막 페이지에 표시하고 온보딩에 머문다 |
| 화면 전체 | 완료 전 앱 종료 후 재실행 | 첫 페이지부터 다시 시작한다. 진행 중이던 페이지 위치는 저장하지 않는다 | 해당 없음 (완료 플래그만 보존 대상이다) |

## 회원가입과 로그인 (Views/Tool/AuthAccountView.swift)

온보딩 계정 페이지, 설정 탭 계정 카드, 세션 게이트에서 같은 화면으로 진입한다.

| 요소 | 동작 | 기대 결과 | 예외 처리 |
| --- | --- | --- | --- |
| 모드 전환 (auth.mode) | 탭 | 로그인과 회원가입 폼을 전환한다. 회원가입 모드에서만 표시 이름과 단위 입력이 나타난다 | 해당 없음 |
| 이메일 필드 (auth.email) | 입력 | 앞뒤 공백을 제거해 전송한다 | @가 없으면 제출 버튼이 비활성화된다 |
| 비밀번호 필드 (auth.password) | 입력 | 마스킹 입력을 받는다. 서버 규칙은 8자 이상 256자 이하다(auth-register.dto.ts) | 8자 미만이면 제출 버튼이 비활성화된다. 상한은 클라이언트에서 정의 없음이며, 257자는 그대로 제출되어 서버 400으로 거부되고 "입력값을 확인해 주세요."가 표시된다 |
| 표시 이름 필드 (auth.display_name) | 입력 | 공백 제거 후 전송한다. 서버 상한은 80자다 | 회원가입 모드에서 비어 있으면 제출 버튼이 비활성화된다. 클라이언트 길이 상한은 정의 없음이다 |
| 제출 버튼 (auth.submit) | 탭 (로그인) | 성공 시 세션을 저장하고 "로그인했어요."를 표시한다. 토큰은 Keychain에 저장된다(SPEC-AUTH-01) | 자격 오류는 "이메일 또는 비밀번호가 맞지 않아요."로 표시하며 계정 존재 여부를 구분하지 않는다. 연결 실패는 서버 연결 확인 안내를 표시한다 |
| 제출 버튼 (auth.submit) | 탭 (회원가입) | 성공 시 세션을 저장하고 "회원가입이 완료됐어요."를 표시한다 | 중복 이메일은 "이미 가입된 이메일이에요. 로그인하거나 다른 이메일을 사용해 주세요."로 표시한다. 상태 코드별로 400, 401, 409, 500 이상을 구분해 안내한다 |
| 계정 정보 영역 (로그인 상태) | 표시 | 표시 이름, 이메일, 사용자 ID, 기본 단위, 서버 저장 상태 배지를 표시한다 | 계정 정보 조회 실패 시 "계정 정보를 새로고침하지 못했어요."와 재시도 버튼을 표시한다 |

## 로그아웃과 세션 게이트 (Views/Tool/AuthAccountView.swift, ContentView.swift)

| 요소 | 동작 | 기대 결과 | 예외 처리 |
| --- | --- | --- | --- |
| 로그아웃 버튼 (auth.logout) | 탭 | 세션만 제거한다(SPEC-AUTH-03). 로컬 데이터는 삭제하지 않고, "로그아웃했어요."를 표시하며 비밀번호 필드를 비운다 | Keychain 삭제 실패 시 세션이 남은 채 아무 안내가 없다. 안내는 정의 없음이다 |
| 세션 게이트 (ContentView) | 세션 없이 앱 진입 | 인증이 요구되는 환경(원격 모드, 정적 토큰 없음)에서는 로컬 캐시를 노출하지 않고 로그인 화면을 표시한다(SPEC-AUTH-06 게이트, SPEC-AUTH-07) | 개발 환경(정적 토큰 주입)에서는 게이트 없이 기존 흐름이 유지된다. 계정 격리의 완전 해소는 미완이며 잔존 결함이 있다(10 문서 DEF-05, DEF-06, DEF-07 부분 수정) |
| 401 응답 수신 | 발생 | 세션만 제거하고 로컬 데이터를 유지한다(SPEC-AUTH-02) | 토큰을 소지한 요청의 401만 세션을 제거한다(DEF-16 수정 반영) |

## 프로젝트 목록 (Views/MyKnitting/MyKnittingView.swift)

| 요소 | 동작 | 기대 결과 | 예외 처리 |
| --- | --- | --- | --- |
| 추가 버튼 (project.add) | 탭 | 프로젝트 추가 시트를 연다 | 해당 없음 |
| 빈 상태 영역 (라벨 "추가") | 탭 | "아직 등록된 프로젝트가 없어요." 안내와 함께 같은 추가 시트를 연다 | 서버 연결 실패로 빈 상태인 경우 "서버에 연결하지 못했어요." 안내와 재시도 버튼을 표시한다 |
| 개수 라벨 ("n개 프로젝트") | 표시 | 필터와 무관하게 전체 프로젝트 개수를 표시한다 | 개수 상한은 코드에 없다. 한도는 정의 없음이다(09 문서 TC7도 명세 공백으로 기록) |
| 필터 칩 (전체, CO, WIP, UFO, FO) | 탭 | 선택한 상태의 프로젝트만 목록에 표시한다 | 필터 선택 상태의 화면 이탈 후 보존 여부는 정의 없음이다(화면 내부 상태로만 유지) |
| 목록 정렬 | 표시 | 즐겨찾기 그룹이 상단에 고정되고, 그 안에서 최근 작업 시각 내림차순으로 정렬한다. 작업 이력이 없으면 시작일을 기준으로 삼는다(LocalSampleRepositories.swift 정렬 규칙, DEF-17 수정 반영) | 해당 없음 |
| 프로젝트 행 (project.row.{id}) | 탭 | 작업 공간의 뜨는 중 탭으로 진입한다 | 해당 없음 |
| 프로젝트 행 | 길게 누르기 | 수정과 삭제 메뉴를 표시한다 | 해당 없음 |
| 프로젝트 행 | 왼쪽으로 스와이프 | 삭제, 수정, 정보 보기(project.show_info) 버튼을 표시한다. 정보 보기는 작업 공간 정보 탭으로 진입한다 | 해당 없음 |
| 삭제 확인창 | 삭제 선택 | "프로젝트를 삭제할까요?"를 표시한다. 취소 시 유지하고, 삭제 시 tombstone 처리 후 목록에서 제외한다(SPEC-SYNC-06) | 삭제 실패 시 "프로젝트를 삭제하지 못했어요."를 표시하고 항목을 유지한다 |
| 동기화 배너 | 표시 | 서버 저장 확인이 필요한 프로젝트가 있으면 "계정 저장 확인이 필요한 프로젝트가 있어요."와 재시도 버튼을 표시한다 | 해당 없음 |
| 캐시 폴백 배너 | 표시 | 서버 연결 실패로 로컬 데이터를 보여줄 때 "서버와 연결하지 못해 이 기기에 저장된 내용을 보여주고 있어요."를 표시한다 | 해당 없음 |
| 목록 전체 | 당겨서 새로고침 | 프로젝트, 도안, 재료 목록을 다시 불러온다 | 실패 시 "프로젝트를 불러오지 못했어요."와 다시 시도 버튼을 표시한다 |
| 목록 전체 | 계정 전환 감지 | 세션이 바뀌면 목록을 비우고 새 계정 데이터를 다시 불러온다(SPEC-AUTH-04 대응) | 해당 없음 |

## 프로젝트 추가 폼 (Views/MyKnitting/AddProjectView.swift, ProjectFormView.swift)

| 요소 | 동작 | 기대 결과 | 예외 처리 |
| --- | --- | --- | --- |
| 이름 필드 (project.form.name) | 입력 | 글자 수 카운터(n/30)를 표시한다. 공백 제거 후 1자 이상 30자 이하가 저장 조건이다(SPEC-PROJ-01) | 타이핑이든 붙여넣기든 30자를 넘는 입력은 30자로 잘라 반영한다. 서버도 같은 규칙으로 거부한다(project-save.dto.ts MaxLength 30) |
| 상태 픽커 (project.form.status) | 선택 | CO, WIP, UFO, FO 중 하나를 저장한다 | 해당 없음 |
| 시작일 픽커 | 선택 | 시작일을 저장한다 | 해당 없음 |
| 즐겨찾기 토글 | 켬 | 목록 상단 고정 대상으로 저장한다. 즐겨찾기 토글은 이 폼 안에만 있고 목록 카드에서는 표시만 한다 | 해당 없음 |
| 목표일, 완료일 토글과 픽커 | 켬과 선택 | 선택 사항으로 저장한다. 목표일은 목록 행과 정보 탭에 D 표기로 나타난다 | 해당 없음 |
| 메모 필드 (project.form.memo) | 입력 | 작업 메모를 저장한다 | 길이 상한은 클라이언트와 서버 모두 정의 없음이다 |
| 실 픽커 (project.form.yarn) | 선택 | 연결 시점 정보를 스냅샷으로 저장한다(SPEC-LINK-05). 창고가 비어 있어도 재료 섹션은 표시된다(SPEC-LINK-06) | 해당 없음 |
| 새 실 등록 (폼 내 즉석 생성) | 탭 | 새 실 등록 시트를 연다. 생성된 실은 창고에 저장되고 폼에 자동 연결된다(SPEC-LINK-06) | 생성 실패 시 "실을 저장하지 못했어요."를 표시한다 |
| 바늘 픽커 (project.form.needle)와 즉석 생성 | 선택과 탭 | 실과 같은 규칙으로 동작한다(SPEC-LINK-05, SPEC-LINK-06) | 생성 실패 시 "바늘을 저장하지 못했어요."를 표시한다 |
| 도구 다중 선택과 즉석 생성 | 선택 | 여러 도구를 골라 저장 시 함께 연결한다(SPEC-LINK-08) | 생성 실패 시 "도구를 저장하지 못했어요."를 표시한다 |
| 도안 픽커 (project.form.pattern) | 선택 | 창고 도안을 골라 생성 시점에 복사본으로 연결한다. 추가 폼에만 있다 | 해당 없음 |
| 저장 버튼 (project.save) | 탭 | 로컬에 먼저 저장한 뒤 서버 반영을 시도한다(SPEC-SYNC-01, SPEC-SYNC-02). 성공 시 시트를 닫고 "프로젝트를 추가했어요."를 표시한다 | 이름이 조건에 어긋나면 버튼이 비활성화된다. 저장 실패 시 "프로젝트를 저장하지 못했어요. 입력값과 서버 연결을 확인해 주세요."를 시트 안에 표시한다. 도구 연결만 일부 실패하면 "프로젝트는 추가했지만 일부 도구를 연결하지 못했어요."를 표시한다 |
| 취소 버튼 | 탭 | 입력을 버리고 시트를 닫는다 | 해당 없음 |

## 프로젝트 수정 폼 (Views/MyKnitting/EditProjectView.swift)

작업 화면의 수정 버튼, 목록 롱프레스, 스와이프 세 경로 모두 같은 폼과 데이터로 진입한다(SPEC-LINK-09). 도안 이름 입력을 제외하면 추가 폼과 같은 구성이다.

| 요소 | 동작 | 기대 결과 | 예외 처리 |
| --- | --- | --- | --- |
| 폼 필드 전체 | 입력 | 추가 폼과 같은 규칙을 따른다. 이름만 바꿔 저장해도 실과 바늘 연결은 유지된다 | 추가 폼과 동일하다 |
| 저장 버튼 (project.save) | 탭 | 변경사항을 저장하고 시트를 닫으며 "프로젝트를 수정했어요."를 표시한다 | 실패 시 "프로젝트 변경사항을 저장하지 못했어요. 입력값과 서버 연결을 확인해 주세요."를 표시하고 시트에 머문다 |
| 취소 버튼 | 탭 | 변경을 버리고 원래 값을 유지한다 | 해당 없음 |
| 삭제 버튼 (project.delete) | 탭 | "프로젝트를 삭제할까요?" 확인창을 거쳐 삭제하고 시트를 닫는다 | 실패 시 "프로젝트를 삭제하지 못했어요. 서버 연결을 확인해 주세요."를 표시한다 |

## 작업 공간, 도안 영역 (Views/MyKnitting/ProjectWorkspaceView.swift, Workspace/ProjectPatternFocusView.swift)

작업 공간은 탭 픽커(workspace.tab)로 뜨는 중과 프로젝트 정보를 전환한다. 뜨는 중 탭은 도안 영역과 하단 카운터 시트로 구성된다. 상단에 프로젝트 이름, 상태 배지, 수정 버튼(workspace.project.edit)이 있다. 뷰모델의 오류는 공통 얼럿("오류")으로 표시되고 확인 버튼으로 닫는다.

| 요소 | 동작 | 기대 결과 | 예외 처리 |
| --- | --- | --- | --- |
| 도안 메뉴 (workspace.pattern.focus.menu) | 탭 | PDF 직접 추가 또는 교체, 문서 스캔, 도안 창고, 수동 입력, 크게 보기, 사전과 스킬 찾기, 그리기 삭제, 도안 연결 해제 항목을 표시한다. 등록 입구는 5개이며 어느 경로든 결과 형태가 같아야 한다(SPEC-PAT-01) | 크게 보기(workspace.pattern.open_pdf)는 PDF 파일이 없으면 비활성화된다. 그리기 삭제와 연결 해제는 해당 대상이 있을 때만 표시된다 |
| PDF 직접 추가 (workspace.pattern.direct_import) | 파일 선택 | 보관 결정 시트를 띄워 도안창고 보관과 프로젝트 전용 연결 중 고르게 한다(SPEC-PAT-01 경로 분기) | 파일 선택 실패 시 아무 처리와 안내가 없다. 정의 없음이다 |
| 문서 스캔 (workspace.pattern.scan) | 스캔 완료 | 스캔 결과를 같은 보관 결정 시트로 넘긴다 | 스캔 실패 시 아무 처리와 안내가 없다. 정의 없음이다 |
| 도안 창고 (workspace.pattern.library) | 선택 | 창고 원본은 남기고 복사본을 프로젝트에 연결한다(DEF-13 수정 반영) | 창고 조회 실패 시 "도안 창고를 불러오지 못했어요."를 표시한다 |
| 수동 입력 시트 (workspace.pattern.manual.title, manual.save) | 제목 입력 후 저장 | 파일 없이 제목만으로 도안을 연결한다(SPEC-PAT-06 계열) | 제목이 공백이면 저장 버튼이 비활성화된다. 제목 길이 상한은 클라이언트와 서버 모두 정의 없음이다 |
| 교체 확인창 | 기존 도안이 있는 상태에서 새 도안 연결 시도 | "기존 도안이 있어요. 교체하면 기존 도안에 작성한 드로잉이 삭제돼요. 그래도 교체할까요?"를 표시한다. 취소 시 기존 도안과 드로잉을 유지하고, 교체 확정 시에만 진행한다(SPEC-LINK-01, DEF-10 수정 반영) | 연결 실패 시 "도안을 프로젝트에 추가하지 못했어요."를 표시하고 기존 드로잉을 유지한다 |
| 연결 해제 확인창 (workspace.pattern.unlink) | 해제 선택 | "이 프로젝트에서 도안 연결만 해제돼요. 도안 창고의 원본은 삭제되지 않아요."를 표시하고, 확정 시 복사본과 로컬 드로잉 표시를 비운다(SPEC-PAT-04 전단) | 실패 시 "도안 연결을 해제하지 못했어요."를 표시한다. 해제로 참조가 끊긴 서버 파일의 회수는 운영 리스크로 남아 있다(SPEC-PAT-04 후단, 02 문서 파일 정리 경로 현황) |
| 보기와 그리기 모드 토글 (workspace.pattern.focus.mode_toggle) | 탭 | 보기 모드에서도 드로잉이 표시되고 편집만 차단된다. 편집은 그리기 모드에서만 가능하다(SPEC-PAT-08, DEF-18 수정 반영) | 도안이 없으면 토글이 비활성화된다 |
| 그리기 캔버스 | 선 긋기 | 변경분이 자동 저장된다. 드로잉은 복사본에 종속된다(SPEC-PAT-03) | 도안이 없으면 "도안을 추가한 뒤 그리기를 사용할 수 있어요."를 표시한다. 저장 실패 시 "그리기를 저장하지 못했어요."를 표시한다 |
| 그리기 삭제 확인창 | 삭제 선택 | "저장된 그리기 메모는 복구할 수 없어요."를 표시하고 확정 시에만 삭제한다 | 실패 시 "그리기를 삭제하지 못했어요." 계열 오류를 얼럿으로 표시한다 |
| PDF 뷰어 페이지 | 페이지 이동 후 화면 이탈과 복귀 | 프로젝트별 키로 페이지 번호를 저장해 복귀 시 유지한다(SPEC-PAT-05, DEF-19 수정 반영). 확대율과 페이지 내 스크롤 위치는 보존 대상이 아니다(SPEC-PAT-05) | 해당 없음 |

## 작업 공간, 카운터 (Views/MyKnitting/ProjectWorkspaceView.swift, Workspace/ProjectCounterPanelView.swift)

하단 시트를 손잡이 탭(workspace.counter.sheet_toggle)이나 드래그로 여닫는다. 접힌 상태는 감소, 현재값, 직접 입력 세 영역의 간이 바를 표시하고, 펼치면 전체 패널과 작업 시간 패널이 나타난다.

| 요소 | 동작 | 기대 결과 | 예외 처리 |
| --- | --- | --- | --- |
| 증가 버튼 (workspace.counter.next) | 탭 | 1회당 1 증가한다. 상한은 없다(SPEC-CNT-01) | 해당 없음 |
| 감소 버튼 (workspace.counter.previous) | 탭 | 1회당 1 감소하며 하한은 0이다(SPEC-CNT-01) | 값이 0이면 버튼이 비활성화된다(DEF-15 수정 반영) |
| 현재값 표시 (workspace.counter.current) | 표시 | 0이면 "시작 전"(간이 바에서는 "시작")으로, 그 외는 단수로 표시한다 | 해당 없음 |
| 값 변경 공통 | 발생 | 값이 바뀔 때마다 개별 카운터 API로 즉시 저장한다. 같은 값이면 저장을 생략한다(SPEC-CNT-02) | 저장 실패 시 "단수를 저장하지 못했어요." 얼럿을 표시하고 화면 값은 이전 상태를 유지한다 |
| 직접 입력 시트 (workspace.counter.number_field, number_save) | 숫자 입력 후 저장 | 입력값으로 현재 단수를 바꾼다 | 음수를 입력하면 "음수는 저장할 수 없어 0으로 보정돼요." 안내가 표시되고 저장 시 0으로 보정된다(SPEC-UI-01). 숫자로 해석되지 않는 입력은 기존 값을 유지하며 별도 안내는 정의 없음이다 |
| 총 단수 편집 (workspace.counter.edit_target) | 입력 후 저장 | 총 단수를 저장하고 진행률 표시를 켠다 | 0 이하 입력은 미설정으로 처리한다 |
| 목표 도달 | 증가로 총 단수 도달 | "프로젝트 상태를 FO - 완성으로 변경할까요?" 확인창을 표시하고, 확정 시 상태를 FO로 바꾸고 완료일을 채운다 | 이미 FO 상태면 묻지 않는다. 변경 실패 시 "프로젝트를 완료로 변경하지 못했어요."를 표시한다 |
| 단수 리셋 버튼 | 탭 | "현재 단수가 0단으로 돌아가요. 행안내 내용은 유지돼요." 확인창을 거쳐 0으로 되돌린다 | 값이 0이면 버튼이 비활성화된다 |
| 모드 픽커 (workspace.counter.mode) | 선택 | 간편 모드와 행안내 모드를 전환해 저장한다. 행안내 모드에서 행 지시 추가(workspace.row_instruction.add), 일괄 입력, 재번호 기능이 나타난다 | 전환 저장 실패 시 "카운터 모드를 저장하지 못했어요."를 표시한다 |
| 섹션 필드와 저장 (workspace.counter.section, section_save) | 입력 후 저장 | 섹션 이름을 저장한다. 공백만 입력하면 섹션 없음으로 처리한다 | 실패 시 "섹션을 저장하지 못했어요."를 표시한다 |
| 카운터 메모 (workspace.counter.memo_field, memo_save) | 입력 후 저장 | 카운터에 딸린 메모를 저장한다 | 실패 시 "카운터 메모를 저장하지 못했어요."를 표시한다 |

## 작업 공간, 작업 시간 (Workspace/ProjectWorkTimePanelView.swift, ViewModels/ProjectWorkspaceViewModel.swift)

| 요소 | 동작 | 기대 결과 | 예외 처리 |
| --- | --- | --- | --- |
| 화면 진입 | 발생 | 작업 세션이 자동으로 시작된다 | 사용자 확인 절차는 없다. SPEC-TIME-01은 확인 또는 임계 시간을 요구하며, 코드의 장치는 아래 최소 시간 조건뿐이다 |
| 화면 이탈 | 발생 | 진행 중이던 세션이 자동으로 종료되고 저장된다. 종료 시각으로 최근 작업 시각이 갱신되어 목록 정렬에 반영된다(DEF-17 수정 반영) | 경과가 10초 미만인 세션은 저장하지 않는다(ProjectWorkspaceViewModel의 최소 시간 조건). 화면을 열어둔 채 방치한 세션을 걸러내는 규칙은 정의 없음이다 |
| 시작과 종료 버튼 (workspace.work_time.start, finish) | 탭 | 자동 흐름과 별개로 세션을 수동으로 시작하고 종료할 수 있다 | 저장 실패 시 "작업 시간을 저장하지 못했어요."를 표시한다 |
| 세션 목록 버튼 (workspace.work_time.sessions) | 탭 | 저장된 세션 목록을 열어 메모 기록과 삭제를 할 수 있다 | 실패 시 세션을 찾지 못했다는 안내 또는 저장 실패 안내를 표시한다 |

## 작업 공간, 정보 탭 (Workspace/ProjectWorkspaceSummaryView.swift 외 패널)

| 요소 | 동작 | 기대 결과 | 예외 처리 |
| --- | --- | --- | --- |
| 프로젝트 요약 | 표시 | 진행(현재와 총 단수), 일정, 시작일, 총 작업시간 네 항목을 표시한다 | 총 단수가 없으면 미설정으로 표시한다 |
| 일정 표시 | 표시 | 목표일까지 남은 날을 D 빼기 형식으로, 당일은 D-Day로, 지난 날은 D 더하기 형식으로 표시한다. 완료된 프로젝트는 완료일을 표시한다 | 목표일이 없으면 목표일 없음으로 표시한다 |
| 진행 사진 (workspace.progress_photo.add 외) | 추가, 수정, 삭제 | 촬영일과 설명을 붙여 저장하고 재실행 후에도 유지한다 | 실패 시 각각 "진행 사진을 저장하지 못했어요.", "수정하지 못했어요.", "삭제하지 못했어요."를 표시한다 |
| 실 사용 기록 (workspace.yarn_usage.record, quantity, memo, save) | 기록 | 사용 수량과 메모를 저장하고 창고 실 수량을 차감한다. 표시는 연결 시점 스냅샷을 쓴다(SPEC-LINK-05) | 수량이 1 미만이면 "사용 수량은 1개 이상이어야 해요."를 표시한다. 연결된 실이 없으면 "프로젝트에 실을 먼저 연결해 주세요."를 표시한다. 삭제 시 "삭제하면 차감했던 실 수량이 다시 복구돼요." 확인창을 거친다 |
| 추가 실과 바늘 연결 (SPEC-LINK-07) | 연결과 해제 | 대표 1개 외에 여러 개를 연결하고 각 연결은 시점 스냅샷을 저장한다 | 오프라인이면 "추가 연결은 서버에 연결된 상태에서 표시돼요."를 표시하고 기능을 막는다. SPEC-LINK-07의 오프라인 지원 기준과는 불일치 확정으로 기록돼 있다(02 문서) |
| 바늘 패널 (workspace.needle.link) | 연결 해제 | "바늘 창고 정보는 유지돼요." 확인창을 거쳐 프로젝트 연결만 해제한다 | 실패 시 "바늘 연결을 해제하지 못했어요. 서버 연결을 확인해 주세요."를 표시한다 |
| 도구 패널 (workspace.tool.link) | 연결과 해제 | "프로젝트에서만 연결이 해제되고 도구 창고 정보는 유지돼요." 확인창을 거친다 | 실패 시 "도구를 프로젝트에 연결하지 못했어요." 또는 "도구 연결을 해제하지 못했어요."를 표시한다 |
| 게이지 기록 연결 (workspace.gauge_record.link) | 연결과 해제 | 연결 개수 표시가 바뀌고 해제해도 게이지 원본은 남는다 | 실패 시 "게이지 기록을 연결하지 못했어요." 계열 안내를 표시한다 |
| 프로젝트 메모 (workspace.memo.field, memo.save) | 입력 후 저장 | 메모를 저장하고 재실행 후에도 유지한다 | 실패 시 "작업 메모를 저장하지 못했어요."를 표시한다. 길이 상한은 정의 없음이다 |
| 관련 스킬 섹션 | 표시 | 행안내와 메모의 약어에서 스킬을 추려 표시하고, 탭하면 상세로 이동한다. 삭제된 스킬 ID는 표시에서 제외된다(SPEC-LINK-03) | 조회 실패 시 "관련 스킬을 불러오지 못했어요."를 표시한다 |

## 창고 (Views/Library/LibraryView.swift, YarnLibraryView.swift, NeedleLibraryView.swift, ToolLibraryView.swift)

창고 탭은 도안, 실, 바늘, 도구, 스킬 다섯 카드로 진입하며 각 카드에 보유 개수를 표시한다. 스킬 창고는 범위 외다.

| 요소 | 동작 | 기대 결과 | 예외 처리 |
| --- | --- | --- | --- |
| 실 등록 (library.yarn.add, 폼) | 저장 | 이름이 필수이고 브랜드, 색상, 굵기, 메모는 선택이다. 수량은 스테퍼로 0에서 999 사이를 받는다 | 이름이 공백이면 저장 버튼(library.yarn.save)이 비활성화된다. 저장 실패 시 "실을 저장하지 못했어요."를 표시한다. 이름과 텍스트 필드의 길이 상한은 클라이언트와 서버(library-save.dto.ts) 모두 정의 없음이다 |
| 실 수정 (library.yarn.edit) | 저장 | 창고 목록에 반영된다. 이미 연결된 프로젝트의 스냅샷에는 전파되지 않는다(SPEC-LINK-05) | 등록과 동일하다 |
| 실 삭제 (library.yarn.delete) | 확인 후 삭제 | "실을 삭제할까요?" 확인창을 거쳐 tombstone 처리하고 목록에서 제외한다. 연결된 프로젝트의 참조와 스냅샷은 남아 동일하게 표시된다(SPEC-LINK-04) | 실패 시 "실을 삭제하지 못했어요."를 표시한다. 확인창에 연결 중인 프로젝트가 있다는 고지는 정의 없음이다 |
| 바늘 등록 (library.needle.add, 폼) | 저장 | 이름, 종류, 사이즈가 필수다. 사이즈는 표준값 퀵픽 20종과 자유 입력을 함께 제공한다(SPEC-UI-01) | 필수값이 비면 저장 버튼이 비활성화된다. 실패 시 "바늘을 저장하지 못했어요."를 표시한다. 길이 상한은 정의 없음이다 |
| 바늘 수정과 삭제 | 저장과 확인 후 삭제 | 실과 같은 규칙을 따른다. 수정은 스냅샷에 전파되지 않고, 삭제해도 연결 프로젝트의 스냅샷은 남는다(SPEC-LINK-04, SPEC-LINK-05) | 실과 동일한 형태의 실패 안내를 표시한다 |
| 도구 등록 (library.tool.add, 폼) | 저장 | 이름과 종류가 필수이고 링크와 메모는 선택이다 | 필수값이 비면 저장 버튼이 비활성화된다. 실패 시 "도구를 저장하지 못했어요."를 표시한다. 길이 상한은 정의 없음이다 |
| 도구 수정 (library.tool.edit) | 저장 | 도구는 스냅샷이 아닌 참조 연결이라 수정이 프로젝트 정보 탭 표시에도 반영된다 | 등록과 동일하다 |
| 도구 삭제 (library.tool.delete) | 확인 후 삭제 | "도구를 삭제할까요?" 확인창을 거쳐 삭제하며, 연결돼 있던 프로젝트의 표시에서도 사라진다 | 실패 시 "도구를 삭제하지 못했어요."를 표시한다 |
| 창고 도안 삭제 (도안 카드 내부) | 확인 후 삭제 | "Library 원본만 삭제돼요. 프로젝트에 가져온 도안 사본은 유지돼요." 확인창을 거쳐 원본만 삭제한다 | 해당 없음 |

## 재방문 보존 (앱 종료 후 재실행)

특정 화면이 아니라 상황 단위로 정의한다. 근거 판정은 09 문서 UI-64와 UI-24, UI-48이다.

| 상황 | 동작 | 기대 결과 | 예외 처리 |
| --- | --- | --- | --- |
| 앱 종료 후 재실행 | 발생 | 카운터 값, 도안 연결, 드로잉, 재료 스냅샷, 메모, 목표일, 즐겨찾기 등 저장된 상태가 그대로 표시된다(SPEC-SYNC-04) | 로컬 파일과 저장된 데이터가 모두 없으면 오프라인 에러를 낸다(SPEC-SYNC-04) |
| 재실행 직후 | 발생 | 보류 중 업로드의 자동 재시도는 없다. 재개는 조회 시점에만 일어난다(SPEC-SYNC-07) | 해당 없음 |
| 도안 뷰어 재진입 | 발생 | 프로젝트별로 저장한 페이지 번호를 복원한다(SPEC-PAT-05). 확대율과 스크롤 위치는 복원하지 않는다 | 해당 없음 |
| 온보딩 완료 여부 | 발생 | 완료 플래그가 보존되어 완료한 사용자는 온보딩을 다시 보지 않는다 | 해당 없음 |

## 오프라인 생성 후 동기화

근거 판정은 09 문서 UI-78과 API-18에서 API-20이다.

| 상황 | 동작 | 기대 결과 | 예외 처리 |
| --- | --- | --- | --- |
| 오프라인에서 프로젝트 저장 | 발생 | 로컬 저장은 성공하고 카드에 "이 기기에만 있음" 배지가 표시된다(SPEC-SYNC-01, SPEC-SYNC-02) | 해당 없음 |
| 네트워크 복구 후 목록 조회 | 발생 | 보류 항목의 업로드를 먼저 시도하고 성공 시 배지가 서버 저장됨으로 바뀐다(SPEC-SYNC-04, SPEC-SYNC-07) | 원격 조회 실패 시 로컬을 반환하고 캐시 폴백 배너를 표시한다 |
| 서버가 업로드를 거부 | 발생 | 로컬 데이터를 삭제하지 않고 확인 필요 상태로 보존한다(SPEC-SYNC-09, SPEC-SYNC-10, DEF-03과 DEF-04 수정 반영). 카드 배지가 확인 필요로 표시된다 | 해당 없음 |
| 미동기화 항목 삭제 | 발생 | 로컬 tombstone을 만들고, 서버에 없던 항목은 즉시 제거하며, 서버에 있던 항목은 원격 삭제 성공 시에만 물리 제거한다(SPEC-SYNC-06) | 원격 삭제 거부 시 항목을 복원한다(SPEC-SYNC-06) |
| 동시 수정 충돌 | 발생 | MVP에서는 나중에 저장한 쪽이 이긴다(SPEC-SYNC-11 조건부 허용). 자식 레코드(작업 세션)는 증분 반영으로 보존된다(SPEC-SYNC-08, DEF-01과 DEF-02 수정 반영) | 충돌 감지와 병합 UI는 정의 없음이다(SPEC-SYNC-11의 한시 허용 전제) |

## 범위 외

다음 기능은 이 문서에서 다루지 않는다. 홈 대시보드, 게이지 계산기, 게이지 측정과 스와치, 게이지 목표, 스킬 테스트, 스킬 애니메이션, 뜨개 사전, 뜨개니게이션, 스킬 창고와 스킬 등록, 프로필 설정, 데이터 백업과 내보내기, 데이터 관리, 작업 시간 통계 화면.

## 정의 없음 총목록

코드에서 처리를 확인하지 못한 항목이다. 이 목록이 이 문서의 핵심 산출물이며, 후속 스펙 결정이 필요한 지점이다.

| 화면 | 요소 | 상황 |
| --- | --- | --- |
| 회원가입 | 비밀번호 필드 | 상한(256자)의 클라이언트 사전 차단이 없다. 초과분은 서버 400 후처리에만 의존한다 |
| 회원가입 | 표시 이름 필드 | 클라이언트 길이 상한이 없다. 서버 상한 80자만 있다 |
| 로그아웃 | 로그아웃 버튼 | Keychain 삭제 실패 시 세션이 남은 채 사용자 안내가 없다 |
| 프로젝트 목록 | 목록 | 프로젝트 개수 상한이 없다 |
| 프로젝트 목록 | 필터 칩 | 필터 선택 상태의 화면 이탈 후 보존 여부가 정의되지 않았다 |
| 프로젝트 폼 | 메모 필드 | 길이 상한이 클라이언트와 서버 모두 없다 |
| 작업 공간 도안 | PDF 직접 추가 | 파일 선택 실패 시 아무 처리와 안내가 없다 |
| 작업 공간 도안 | 문서 스캔 | 스캔 실패 시 아무 처리와 안내가 없다 |
| 작업 공간 도안 | 수동 입력 제목 | 길이 상한이 클라이언트와 서버 모두 없다 |
| 작업 공간 카운터 | 직접 입력 시트 | 숫자로 해석되지 않는 입력이 안내 없이 기존 값으로 대체된다 |
| 작업 공간 작업 시간 | 자동 세션 | 화면을 열어둔 채 방치한 세션을 걸러내는 규칙이 없다. 하한 10초 조건만 있다 |
| 작업 공간 정보 탭 | 프로젝트 메모 | 길이 상한이 클라이언트와 서버 모두 없다 |
| 창고 | 실, 바늘, 도구 폼의 텍스트 필드 | 이름을 포함한 텍스트 길이 상한이 클라이언트와 서버 모두 없다 |
| 창고 | 실 삭제 확인창 | 연결 중인 프로젝트가 있다는 고지가 없다 |
| 동기화 | 동시 수정 | 충돌 감지와 병합 안내가 없다. SPEC-SYNC-11이 한시 허용으로 기록한 전제 그대로다 |
