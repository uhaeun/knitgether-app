# 12_품질 지표 스코어카드

> 2026-08-30 기준. 모든 수치는 리포지토리 파일과 앞 문서에서 직접 세었다.
> 미실측 항목은 숫자를 쓰지 않고 빈칸으로 둔다. 추정값을 넣지 않는 것이 00의 근거 실측화 원칙이다.

측정 시점과 기준이 항목마다 다르므로 각 표에 밝힌다.

- 1차 실행 판정: 2026-08-04~05, 기준 빌드 `bf2b001`(트랙 A) / UX 개편 빌드(트랙 B, C)
- 2차 케이스 설계: 2026-08-23~24, 구글시트
- 코드 실측: 2026-08-27, 커밋 `e4de8cb`
- 2차 자동화 실행: 2026-08-29~30 (Appium 분할 실행 3회, API 스위트 37건)

---

## 1. 설계 규모

| 항목 | 수 | 출처 |
| --- | --- | --- |
| SPEC (검증 기준선) | 40 (02 집계 표기는 42) | 02 본문 행 실측 |
| 결함 클래스 (RC) | 8 (P1 2, P2 3, P3 2, 별도 관리 1) | 03 |
| CUJ | 4 | 01 |
| 1차 TC | 21 | 05 |
| 2차 TC 그룹 | 16 | 09 |
| 2차 UI 케이스 | 78 | 09 |
| 2차 API 케이스 | 25 | 09 |

SPEC 40 대 42 불일치: 02의 한눈에 요약과 문서 이력은 PAT 8건을 세어 42로 집계하는데, 본문 SPEC-PAT 표에는 6건(PAT-01, 02, 03, 04, 05, 08)만 있다. 이력 v2.0에 신설로 기록된 PAT-06과 PAT-07의 행이 없다. 최종 페이지에 쓸 숫자이므로 확정 전까지 40으로 적고 괄호로 병기한다.

### SPEC 영역별 (본문 실측 40건)

| 영역 | 건수 |
| --- | --- |
| SYNC 데이터 저장과 동기화 | 11 |
| LINK 프로젝트-창고 연결 | 9 |
| AUTH 인증 | 7 |
| PAT 도안 | 6 (집계 표기 8) |
| CNT 단수카운터 | 3 |
| UI 입력 보조와 알림 | 2 |
| TIME 작업 시간 | 1 |
| PROJ 프로젝트 입력 | 1 |

### 코드 일치 판정 분포 (실행 전 정적 대조)

| 판정 | 건수 |
| --- | --- |
| 일치 / 그 외 | 19 |
| 불일치 의심 | 10 |
| 불일치 확정 | 5 |
| 미대조 | 4 |
| 조건부 일치 | 2 |

**실행 전에 이미 15건(불일치 의심 10 + 확정 5)을 식별했다.** 02 작성 과정의 코드 실측이 정적 테스트로 기능한 결과이며, 04에서 정적 테스트를 테스트 레벨의 하나로 잡은 근거다.

---

## 2. 1차 실행 결과 (2026-08-04~05)

| 트랙 | 실행 | PASS | FAIL | PASS율 |
| --- | --- | --- | --- | --- |
| A (CUJ 넓이) | 4 | 3 | 1 | 75% |
| B (P1 악조건 심층) | 6 | 0 | 6 | 0% |
| C (표준) | 11 | 9 | 2 | 82% |
| 합계 | 21 | 12 | 9 | 57% |

트랙 B의 0%는 검출률 100%로 읽는다. P1 클래스는 03에서 정상 흐름에 드러나지 않는다고 판단해 악조건을 인위적으로 만든 구간이고, 전건 재현에 성공한 것이다.

이 숫자에 걸린 조건: 트랙 A의 FAIL 1건(TC-CUJ3-01)은 판정 근거가 06에도 시트에도 없다. 11의 3절 참조. 처리 전까지 21 / 12 / 9를 최종 확정 수치로 쓰지 않는다. 미실행으로 판명되면 20 / 12 / 8이 된다.

---

## 3. 결함 (21건)

### 결함 클래스 분포

| 클래스 | 건수 | 해당 |
| --- | --- | --- |
| RC-01 동기화 유실 | 6 | DEF-01, 02, 03, 04, 10, 12 (DEF-10은 8/29 RC-03에서 재분류) |
| RC-02 계정 격리 | 3 | DEF-05, 06, 07 |
| RC-03 표시 정합성 | 2 | DEF-18, 19 |
| RC-03 + 데이터 계약 | 1 | DEF-17 |
| RC-04 데이터 계약 | 1 | DEF-08 |
| RC-07 죽은 ID 표시 | 1 | DEF-09 |
| 파일 정합성 | 2 | DEF-13, 14 |
| 캐시 갱신 전략 | 1 | DEF-11 |
| 세션 정합성 | 1 | DEF-16 |
| 회귀 (UX 개편 유발) | 1 | DEF-15 |
| 명세 공백 → 스펙 신설 | 1 | SPEC-PROJ-01 |
| 검증 공백 | 1 | DEF-20 (8/30 신규, 07 API 트랙) |

P1 클래스(RC-01 + RC-02)가 21건 중 9건, 43%다. (8/29 DEF-10 재분류 전 8건 40%) 03의 등급이 실제 발견 분포와 맞았다는 뜻이며, 리스크 기반 설계가 헛돌지 않았다는 근거다.

### 심각도 분포 (8/29 확정)

| 등급 | 건수 | 해당 |
| --- | --- | --- |
| Critical | 11 | DEF-01~08, 10, 12, 16 |
| Major | 7 | DEF-09, 11, 15, 17, 18, 19, 20 |
| Minor | 0 | |
| 심각도 축 제외 | 3 | DEF-13, 14 (RC-08 운영 리스크, 07로 이관), SPEC-PROJ-01 (스펙 신설) |

심각도 부여 대상 18건 중 Critical 11건, 61%다. 위의 P1 클래스 43%와 숫자가 다른 이유는 축이 다르기 때문이다. 클래스는 어떤 정합성이 깨졌는지를 보고 심각도는 결과가 얼마나 나쁜지를 본다. RC-04(데이터 계약)의 DEF-08은 P1 클래스가 아니지만 목록 전체가 열리지 않아 Critical이고, RC-03의 DEF-18과 19는 표시 정합성이지만 회복 가능해 Major다. 판정 근거는 10의 2-1절, 상향 규칙 개정은 04에 있다.

### 원인 계열로 묶었을 때

| 뿌리 | 파생 결함 |
| --- | --- |
| 원격 캐시 갱신 시 제거 없이 병합 | 3 (DEF-05, 09, 11) |
| 서버 거부 시 무통보 폐기 | 3 (DEF-03, 04, 12) |
| 레코드 하드 삭제 | 2 (DEF-01, 02) + RC-08 |
| 화면 상태가 뷰 인스턴스에만 존재 | 2 (DEF-18, 19) |
| 그 외 개별 | 10 |

21건 중 10건이 4개 뿌리에서 나왔다. 결함 수보다 이 압축률이 수정 범위를 정한 근거다.

### 발견 시점

| 범주 | 건수 |
| --- | --- |
| QA 신규 발견 (05 설계 케이스 실행) | 10 |
| QA 신규 발견 (탐색 관찰, 케이스 밖) | 5 |
| QA 신규 발견 (2차 자동화 준비 단계) | 4 |
| QA 사이클 중 발생한 회귀 | 1 |
| QA 신규 발견 (2차 07 API 트랙) | 1 |

전건 QA 발견이다. 개발 중 발견이나 인수인계 known issue를 QA 발견으로 옮겨 세지 않았다. DEF-01은 known issue #16의 동작을 실측으로 확정한 것이며, 문서에 적혀 있던 것과 데이터가 실제로 사라지는 것을 확인한 것을 구분해 QA 발견으로 뒀다.

### 수정 상태 (시트 8/24 기준)

| 상태 | 건수 |
| --- | --- |
| 수정 완료 (회귀 확인까지) | 4 |
| 수정 완료 (커밋만) | 9 |
| 부분 수정 (잔존) | 3 |
| 미수정 / 미구현 | 4 |

8/30 갱신: 미수정 4건(DEF-17, 18, 19, SPEC-PROJ-01)의 회귀 재판정이 끝났다. UI 회귀 6건(UI-17, 33, 35, 38, 49, 51)과 API-25 4건 전건 PASS다. 회귀 케이스가 `xfail(strict)`이었으므로 수정이 안 먹었다면 지금도 실패해야 하고, 현재 스위트에 xfail은 0개다.

DEF-20은 8/30 신규 발견으로 미수정 상태이며 회귀 감시 케이스가 `test_05`에 고정돼 있다.

---

## 4. 자동화

| 항목 | 수 | 비고 |
| --- | --- | --- |
| Appium 테스트 함수 / 실행 케이스 | 78 / 80 | UI-01~78 전건. UI-04~06이 parametrize 3분기. 실행 79 passed / 1 skipped / 0 failed, 3회 동일 (8/29~30) |
| Appium Page 객체 | 12 | |
| Appium support 모듈 | 6 | |
| XCUITest (QA 작성) | 1 | `testDEF15` + POM 3 |
| 뮤테이션 주입 | 6종 | 6종 전건 검출, 과잉 결합 0건 (8/27). 실제 검출력 공백 UI-64 1건은 음성 대조가 아닌 결함 주입에서 드러남 |
| `--empty-given` 음성 대조 | 53건 FAIL / 검출력 없음 0건 | 서버 케이스 14건에는 신호 없음 (파일 시딩만 차단) |
| API 테스트 | 37건 / 11파일 | 36 passed / 1 skipped (8/30). 전 판정이 psql로 DB 실제 상태 대조 |
| 네트워크 열화 도구 | 5조건 | `netgate.py` TCP 프록시. 단절, 절단, 지연, 대역폭, 방향별 정지 |
| CI 게이트 잡 | 3 | `server` 빌드와 e2e, `api` 계약과 열화와 DB 정합성, `ios` 빌드와 UI 스모크 |
| qa-gate.yml 단계 | 4 + artifact | 빌드, 단위 회귀, DEF-15 UI 회귀 |

### 개발 단계 산출물 (회귀 장치로만 활용, 실적에서 제외)

| 항목 | 수 |
| --- | --- |
| XCUITest (AI 작성) | 23 / 20파일 |
| Swift 단위 `@Test` | 329 / 42파일 |
| 서버 e2e `it()` | 117 / 13스펙 |
| pytest API (초기) | 6파일 |

### 케이스 대 자동화 대응

| | 케이스 | 자동화 대응 | 비율 |
| --- | --- | --- | --- |
| UI | 78 | 78 | 100% |
| API | 25 | 15 덮임 + 5 부분 | 60% (부분 포함 80%) |

UI 100%는 커버리지가 아니라 대응률이다. 함수가 존재한다는 뜻이고, 통과 판정은 아래 실행 결과가 따로 말한다.

API 60%는 대응률이 아니라 덮임 판정이다. 그 케이스의 검증 내용을 실제로 단언하는 테스트가 있는지 기준이고, 부수적으로 지나가기만 하는 것은 부분으로 뒀다. 미커버 5건은 P2 이하이거나 치명도 축 밖이라 근거를 남기고 뺐다(07의 3-2절).

### 실행 결과 (2026-08-29~30)

| 스위트 | 결과 | 회차 |
| --- | --- | --- |
| Appium UI | 79 passed / 1 skipped / 0 failed | 정방향 2회, 역순 1회 전부 동일 |
| API | 36 passed / 1 skipped / 0 failed | CI 포함 다회 |

역순 실행은 파일 순서와 파일 안 케이스 순서를 둘 다 뒤집은 것이다. 앞 케이스가 남긴 상태에 뒤 케이스가 기대고 있으면 정방향으로는 몇 번을 돌려도 드러나지 않으므로, 케이스 간 격리를 확인하려면 순서를 바꿔야 한다.

### 자동화 신뢰성 네 축

통과 자체가 아니라 통과가 무엇을 증명하는지를 축별로 나눴다.

| 무엇을 증명하는가 | 방법 | 결과 |
| --- | --- | --- |
| 스위트 안정성 | 전체 2회 연속 실행 | 동일 |
| 케이스 간 격리 | 순서 역전 실행 | 동일 |
| 검출력 | 결함 6종 주입 대조 | 6종 전건 검출, 과잉 결합 0건 |
| 전제 의존성 | Given 제거 음성 대조 | 검출력 없는 케이스 0건 |

새로 쓴 API 테스트도 같은 원칙을 따랐다. 기대값을 뒤집은 대조본에서 실패하는지 파일별로 확인했다.

| 파일 | 뒤집은 단언 | 대조본 결과 |
| --- | --- | --- |
| `test_07` API-25 | 3 | 3 실패 |
| `test_08` 네트워크 열화 | 4 | 4 실패 |
| `test_10` 인증 계약 | 4 | 4 실패 |
| `test_11` 동기화 유실 | 5 | 5 실패 |

### 플레이키

| 케이스 | 수정 전 | 수정 후 |
| --- | --- | --- |
| UI-61 게이지 연결 해제 | 5회 중 3회 실패 | 5회 전건 통과 |

원인은 대기가 아니라 롱프레스 좌표였다. 제목 텍스트만 눌러 컨텍스트 메뉴가 붙은 행 컨테이너에 닿지 않는 경우가 있었고, 제목 줄과 코수 줄을 합친 영역의 중심을 누르도록 바꿨다. 04의 테스트 가능성 예외를 쓰지 않고 자동화 코드만으로 해소했다.

---

## 5. 04 종료 기준 대조

| 종료 기준 | 상태 |
| --- | --- |
| P1 클래스 2건의 실측 완료와 판정 확정 | 충족 |
| 02의 불일치 의심과 미대조 전건 확정 | 충족 |
| CUJ 4개 실행 완료 | 미확인 (TC-CUJ3-01) |
| 발견 결함 전건 GitHub Issue 등록 | 미확인 (대장에 이슈 번호 열 없음) |

2차 사이클 종료 기준(04 2차 전략)

| 기준 | 상태 |
| --- | --- |
| 스모크 구간(TC1~TC4) 자동화 완주 | 충족 (3회 실행 전건 통과) |
| 미수정 4건 수정과 회귀 재판정 | 충족 (UI 6건 + API-25 4건 전건 PASS) |
| 상태 열 전건 기록 | 충족 (09에 8/29 자동 실행 판정 열 신설) |

---

## 6. 채우지 못한 지표

숫자를 넣지 않고 비워 둔 항목과 그 이유다.

| 지표 | 왜 비었나 | 선행 조건 |
| --- | --- | --- |
| ~~심각도 분포~~ | 8/29 채움, 8/30 DEF-20 반영 (3절) | |
| ~~Appium PASS율~~ | 8/30 확정 79 passed / 1 skipped (4절) | |
| ~~뮤테이션 검출률~~ | 8/27 채움 6/6 (4절) | |
| ~~API 자동화 커버리지~~ | 8/30 채움 15/25 덮임 (4절) | |
| ~~케이스 간 격리~~ | 8/30 채움 역순 실행 동일 (4절) | |
| ~~음성 대조~~ | 8/27 채움 53 FAIL / 검출력 없음 0 (4절) | |
| 결함 밀도 / 재개봉률 | 산정 기준 미정 | QA가 기준 정의 |
| 평균 수정 리드타임 | 발견일과 수정일이 대장에 일부만 있음 | 대장에 날짜 열 보강 |

**이 표 자체가 지표다.** 무엇을 아직 모르는지 적어 두는 것이 모르는 것을 추정치로 채우는 것보다 낫다.

---

## 7. 남은 판단 (QA가 채운다)

1. ~~심각도 부여~~ 8/30 완료. 21건 중 Critical 11 / Major 7 / 축 제외 3. 상향 규칙은 04에서 개정했고 DEF-20은 8/30 Major로 확정
2. SPEC 40 대 42 확정. PAT-06, PAT-07을 본문에 채울지 집계를 40으로 내릴지
3. 1차 집계 21 / 12 / 9 확정. TC-CUJ3-01 처리 후
4. 결함 밀도와 재개봉률을 이 사이클에서 낼지. 1인 QA 단일 사이클이라 비교 기준이 없다. 낼 수 없으면 안 내는 것도 판단이다
