# 10_결함 카탈로그

> 원본: 구글시트 [뜨개더 TC 설계](https://docs.google.com/spreadsheets/d/1EjP0M0_BcFdMvJmjDk8-u-56_toANB4j084Hs7Bh4mI/) `결함 대장` 탭 (최종 갱신 2026-08-24).
> 재현 절차 전문, psql 원문, 발견 시각은 06과 그 부록이 정본이다. 이 문서는 결함의 계보와 상태를 한자리에 모은 카탈로그다.

`수정 커밋` 열의 해시는 2026-08-27에 리포지토리에서 전건 실재 확인했다. **커밋이 존재한다는 사실이지 회귀 검증이 끝났다는 판정이 아니다.** 회귀 판정은 `회귀 담당` 열의 케이스를 실행해야 나온다.

---

## 1. 발견 시점 분류

00에서 선언한 세 범주다.

| 범주 | 건수 | 해당 |
| --- | --- | --- |
| QA 신규 발견 (05 설계 케이스 실행) | 10 | DEF-01~05, 07~10 |
| QA 신규 발견 (탐색 관찰, 케이스 밖) | 5 | DEF-06, 11, 12, 13, 14 |
| QA 사이클 중 발생한 회귀 | 1 | DEF-15 |
| QA 신규 발견 (2차 자동화 준비 단계) | 4 | DEF-16~19 |
| 명세 공백 → 스펙 신설 | 1 | SPEC-PROJ-01 |

DEF-01은 인수인계 문서 known issue #16의 동작을 실측으로 확정한 것이다. 문서에 적혀 있던 것과 실제로 데이터가 사라지는 것을 확인한 것은 구분한다.

---

## 2. 결함 대장 (DEF-01 ~ DEF-19, SPEC-PROJ-01)

| ID | 제목 | 분류 | 발견 시점 | 발견 경위 | 상태 (시트 8/24) | 수정 커밋 (repo 실측 8/27) | 회귀 담당 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DEF-01 | 다중 기기 충돌로 작업 세션 하드 삭제 | RC-01 동기화 유실 | 8월 초 (06 수동) | TC-SYNC08-01 실행 | 수정 완료 | `d0a753a` 서버 증분 upsert (8/9) | API-18 |
| DEF-02 | 단일 기기 재전송으로 세션 소멸 | RC-01 동기화 유실 | 8월 초 (06 수동) | TC-SYNC08-02 실행 | 수정 완료 | `d0a753a` 계열 | API-18 |
| DEF-03 | 서버 거부 시 localOnly 완전 유실 | RC-01 동기화 유실 | 8월 초 (06 수동) | TC-SYNC09-01 실행 | 수정 완료 | `eab799d` conflict 보존 (8/9) | API-19 |
| DEF-04 | pendingUpload 수정본 무통보 폐기 | RC-01 동기화 유실 | 8월 초 (06 수동) | TC-SYNC10-01 실행 | 수정 완료 | conflict 보존 계열 | API-20 |
| DEF-05 | 계정 전환 시 타 소유자 데이터 노출 | RC-02 계정 격리 | 8월 초 (06 수동) | TC-AUTH04-01 실행 | **부분 수정 (잔존)** | 캐시 프루닝 계열 | UI-77 |
| DEF-06 | 로그아웃 시 익명 영역으로 캐시 유입 | RC-02 계정 격리 | 8월 초 (탐색 관찰) | DEF-05 조사 중 캐시 폴더 실측 | **부분 수정 (잔존)** | `4d76203` AUTH-06 게이트 (8/11) | UI-77 |
| DEF-07 | 타 소유자 프로젝트 화면 표시 (업로드는 격리됨) | RC-02 계정 격리 | 8월 초 (06 수동) | TC-AUTH05-01 실행 | **부분 수정 (잔존)** | DEF-05와 동일 계열 | UI-77 |
| DEF-08 | RowCounter 없는 프로젝트가 목록 전체 500 | RC-04 데이터 계약 | 8월 초 (06 수동) | TC-CNT03-01 실행 | 수정 완료 (회귀 확인) | `06c7a10` 목록 격리 (8/6) | API-21 |
| DEF-09 | 삭제된 스킬이 로컬 캐시에 잔존 표시 | RC-07 죽은 ID 표시 | 8월 초 (06 수동) | TC-LINK03-01 실행 | 수정 완료 | 캐시 프루닝 계열 | API-22 |
| DEF-10 | 도안 교체 시 확인창 없이 드로잉 삭제 | RC-03 표시 정합성 | 8월 초 (06 수동) | TC-LINK01-01 실행 | 수정 완료 (회귀 확인) | `8f1a4ad` 네 진입점 확인창 (8/9) | UI-45 |
| DEF-11 | 원격 캐시가 서버 삭제 항목을 제거하지 않음 | 캐시 갱신 전략 | 8월 초 (탐색 관찰 31) | psql 삭제 후 잔존 확인 | 수정 완료 | 캐시 스테일 제거 | UI-28 간접 |
| DEF-12 | 자가 치유 중 수정분 폐기와 소멸 무통보 | RC-01 동기화 유실 | 8월 초 (탐색 관찰 32) | DEF-11 재현 중 관찰 | 수정 완료 | conflict 보존 계열 | API-20 |
| DEF-13 | 창고 경로 도안의 서버 파일 키 부재 | 파일 정합성 | 8월 초 (탐색 관찰) | 07 추적 | 수정 완료 | `a74945a` (8/11) | API-23, UI-43 |
| DEF-14 | 도안 교체 후 서버 드로잉 키 잔존 | 파일 정합성 | 8월 초 (탐색 관찰 37) | psql 키 확인 | 수정 완료 | `0109898` 레코드 재생성 (8/9) | API-24 |
| DEF-15 | 카운터 0에서 감소가 증가로 오동작 | 회귀 (UX 개편 유발) | 8월 초 (06 실행 중) | TC-CNT01-01 실행 | 수정 완료 (자동화) | `34f3c00` 겹침 제거 (8/6) | UI-39, UI-54 + CI 게이트 |
| DEF-16 | 로그인 직후 무관한 401이 새 세션 삭제 (경합) | 세션 정합성 | 8/22~23 (자동화 준비) | 실기기 탐색 + 서버 계측 | 수정 완료 (검증) | `4f7e25e` 토큰 소지 요청만 삭제 (8/23) | UI-08 (증상 감시) |
| DEF-17 | 작업 세션 저장 시 lastWorkedAt 미갱신 | RC-03 표시 정합성 + 데이터 계약 | 8/23 (케이스 설계) | UI-33 설계 중 정렬 관찰 → 코드 대조 → `projects.json` 실증 | 미수정 | **`543ae7f` (8/26, 시트 미반영)** | UI-33, 35, 38, API-25 |
| DEF-18 | 보기 모드에서 드로잉 미표시 | RC-03 표시 정합성 | 8/23 (케이스 설계) | UI-48 실행 중 관찰 | 미수정 | **`d1c6010` (8/26, 시트 미반영)** | UI-49 |
| DEF-19 | 탭 복귀 시 도안 페이지 1페이지로 초기화 | RC-03 표시 정합성 | 8/23 (케이스 설계) | UI-51 실행 중 관찰 | 미수정 | **`d1c6010` (8/26, 시트 미반영)** | UI-51 |
| SPEC-PROJ-01 | 프로젝트 이름 길이 제한 미구현 (1~30자 신설) | 명세 공백 → 스펙 신설 | 8/23 (케이스 설계) | UI-17 설계 중 코드 3층 확인 | 미구현 | **`834876f` (8/26, 시트 미반영)** | UI-17 |

---

## 3. 원인 계열 (개별 버그가 아니라 뿌리로 묶기)

06의 핵심 발견이다. FAIL 9건이 개별 화면 버그의 나열이 아니라 두 뿌리에서 파생됐다.

| 뿌리 | 결함 | 무엇이 같은가 |
| --- | --- | --- |
| 원격 캐시 갱신 시 제거 없이 병합만 함 | DEF-05, DEF-09, DEF-11 | 서버에서 사라진 것이 로컬에 남는다. 계정 유입, 죽은 스킬, 유령 프로젝트가 전부 같은 전략 결함의 표면 |
| 서버 거부 시 무통보 폐기 | DEF-03, DEF-04, DEF-12 | 거부 응답이 로컬 데이터의 삭제나 덮어쓰기를 유발하고 사용자에게 알리지 않는다 |
| 레코드 하드 삭제 (deletedAt 마킹 부재) | DEF-01, DEF-02, RC-08 파일 고아화 | 자식 레코드를 지울 때 tombstone 없이 물리 삭제한다. 서버 파일 키까지 함께 소멸해 회수 경로가 사라진다 |
| 화면 상태가 뷰 인스턴스에만 존재 | DEF-18, DEF-19 | 드로잉 캔버스와 PDF 페이지 위치가 뷰모델이나 저장소에 없어 뷰 재생성 시 소실 |

수정 범위와 우선순위는 개별 결함이 아니라 이 뿌리 단위로 정했다.

---

## 4. 이 사이클이 남긴 두 가지 함정 사례

**1. AI가 작성한 단위 테스트가 결함을 정상으로 단언했다**

구현에서 역산해 AI가 쓴 단위 테스트(`rejectedPendingLocalOnly`)가, QA가 FAIL로 판정한 데이터 유실 동작(SYNC-09 / DEF-03)을 기대 동작으로 고정하고 있었다. 테스트가 통과한다는 사실이 결함이 없다는 근거가 되지 못한 사례다. 2차 회귀에서 같은 테스트가 유실을 다시 가린 이중 함정도 확인했다.

**2. 두 개의 옳은 수정이 조합에서 서로를 무효화했다**

캐시 프루닝(DEF-11 수정)과 conflict 보존(DEF-03 수정)이 각각은 맞는 수정이었으나, 프루닝이 보존된 conflict를 지워버렸다. 2차 회귀에서 발견해 `eab799d`로 해소했다. 개별 수정의 단위 검증만으로는 잡히지 않는 유형이다.

---

## 5. 남은 판단 (QA가 채운다)

1. **8/26 수정 4건의 회귀 재판정** — DEF-17, 18, 19, SPEC-PROJ-01은 수정 커밋이 들어왔으나 `회귀 담당` 케이스(UI-33, 35, 38, 49, 51, 17, API-25)를 실행한 기록이 없다. 실행 전까지 상태는 "수정 커밋 존재, 회귀 미판정"이다
2. **RC-02 잔존 3건(DEF-05, 06, 07)의 릴리즈 판정** — 게이트로 노출은 막았으나 저장 구조는 그대로다. 계정 격리는 치명도 3위이고 04 심각도 기준으로는 Critical에 해당한다. 부분 수정 상태를 릴리즈 가능으로 볼지가 이 사이클의 최종 판정 항목
3. **심각도 열 부재** — 04에서 Critical / Major / Minor 3단계를 정의했으나 대장에 심각도 열이 없다. 12 스코어카드가 심각도 분포를 요구하므로 20건 전건 부여 필요
4. **GitHub Issue 등록 상태** — 04 종료 기준에 "발견 결함 전건 GitHub Issue 등록"이 있다. 대장에 이슈 번호 열이 없어 충족 여부를 이 문서만으로는 확인할 수 없다
