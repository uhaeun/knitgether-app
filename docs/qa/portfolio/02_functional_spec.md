# 02_기능 동작 정의서

> 원본: Notion `02_기능 동작 정의서` (v2.1, 최종 갱신 2026-08-24) / repo 이관 2026-08-27
> 미반영 후속: Notion `02 v1.5 개정 초안 (Claude 작성, 반영 전)` — 본문 미반영 상태 그대로 이관했다.

#### 한눈에

- 이 문서가 보여주는 것: 명세 공백 상황에서 검증 기준선 수립, 코드와 DB 실측 기반 분석, 요구사항 추적(RTM) 체계 운영
- 세 줄 요약: 사전 명세가 구현과 괴리된 상태에서 코드 실측과 제품 판단으로 SPEC 42건의 검증 기준선을 세웠다. 각 SPEC에 코드 일치 여부를 5단계로 기록했고, 1차 사이클에서 불일치 의심 8건을 실행 전에 식별했다. 모든 SPEC은 상위(CUJ)와 하위(테스트케이스)로 추적된다. (SYNC 11, LINK 9, CNT 3, PAT 8, AUTH 7, TIME 1, UI 2, PROJ 1)

#### 이 문서는 무엇이고 무엇이 아닌가

이 문서는 기획자가 개발 전에 쓰는 전통적 기능명세서가 아니다. QA가 개발 완료 후에 수립한 검증 기준선(test basis)이며, 관찰 가능한 동작을 판정 가능한 문장으로 정의하고 테스트 케이스 도출의 근거로 쓴다. 코드에서 역산했으나 코드를 정답으로 삼지 않는다. 코드 동작과 기획 의도가 어긋나는 항목은 불일치로 기록하고, 코드에도 답이 없는 동작은 명세 공백으로 분리해 관리한다.

이 프로젝트의 문서 체계는 세 층이다. 사전 기획 문서가 개발에 선행해 의도와 요구사항을 정의했고 회차별 구현과 병행하며 갱신됐다. 그에 따라 MVP가 구현됐으며, 이 문서는 QA 단계에서 수립한 검증 기준선이다. 검증 기준선은 개별 동작을 판정 가능한 문장으로 정의하는 문서로, 의도 수준의 사전 문서보다 정밀도가 한 단계 깊다.

기준선의 원천은 QA 인수인계 문서와 현재 코드다. 인수인계 문서는 개발 완료 시점(기준 커밋 bf2b001)에 작성된 시스템 안내서로 아키텍처, 데이터 흐름, API 목록, 알려진 이슈(known issues)를 담는다. 사전 기획 문서는 구현 과정에서 데이터 구조가 전면 변경되어 현재 코드와 괴리가 있으므로 의도 참고용으로만 쓰고, 동작 정의는 코드 실측에서 역산했다. 코드 동작을 그대로 기준으로 승인할지, 기대와 어긋나 불일치로 기록할지는 기획자 관점에서 항목마다 판정해 확정했다. 근거 열의 파일:라인은 모두 기준 커밋 bf2b001 시점 기준이다.

이 문서는 요구사항 추적(RTM)의 가운데 층이다. 상위는 01의 CUJ와 치명도 축이고, 하위는 05의 테스트케이스다. 각 SPEC은 관련 CUJ를 부록에서 매핑하며, 05의 모든 TC는 SPEC ID를 참조한다. 요구사항부터 실행 결과까지의 전체 추적 매트릭스는 05에서 조립하고 06의 판정 결과와 연결한다.

이 기준선은 기능 동작에 한정한다. 비기능 요구사항(성능, 보안, 접근성)은 00에서 선언한 범위 판단에 따라 이번 사이클에서 다루지 않으며, 근거는 04 테스트전략에서 서술한다. 단 인증 관련 동작(SPEC-AUTH)은 보안 항목이 아니라 데이터 정합성 관점의 기능 동작으로 다룬다.

각 SPEC에는 코드 일치 여부를 함께 기록했다. **일치**는 코드 동작을 그대로 기준으로 채택한 항목, **조건부 일치**는 조건을 달아 채택한 항목, **불일치 의심**은 기획자가 정의한 SPEC과 코드가 어긋나는 것으로 보여 06 실행에서 확정할 항목, **불일치 확정**은 코드 구조만으로 어긋남이 확정된 항목이다. **미대조**는 기획자가 정의한 기대이나 코드 대조를 수행하지 않아 06 실행에서 확인할 항목이다. 여기서 일치는 코드를 정답(test oracle)으로 확정했다는 뜻이 아니다. 테스트 오라클은 코드 자체여서는 안 되므로, 관찰된 코드 동작을 기획 의도와 대조한 뒤에만 기준으로 채택했으며 대조에서 어긋난 것이 불일치 항목들이다. 합격/불합격 판정은 06 테스트 실행에서 한다. 각 항목의 치명도 연결과 우선순위는 03 품질리스크맵에서 매핑한다.

#### SPEC-SYNC 데이터 저장과 동기화

| ID | 동작 정의 | 근거 | 코드 일치 여부 |
| --- | --- | --- | --- |
| SYNC-01 | 저장은 로컬 먼저 반영 후 원격을 시도한다. 원격 실패가 네트워크성이면 pending 유지, 서버 거부면 로컬을 스냅샷으로 복원하고 에러를 알린다 | OfflineFirstProjectRepository.swift:54, :300 | 일치 |
| SYNC-02 | 저장 시 상태 전이는 synced와 pendingUpload가 pendingUpload로, 그 외는 localOnly로 간다. 업로드 성공 시 synced가 된다 | 같은 파일 :325, :334 | 일치 |
| SYNC-03 | 원격 반영은 synced 항목이면 PATCH, 아니면 POST를 쓴다 | RemoteProjectRepository.swift:34 | 일치 |
| SYNC-04 | 조회 시 pending 업로드를 먼저 시도하고, 원격 성공 시 로컬 캐시를 갱신하며, 네트워크 실패 시 로컬을 반환한다. 로컬도 저장 파일도 없으면 오프라인 에러를 낸다 | OfflineFirstProjectRepository.swift:16 | 일치 |
| SYNC-05 | 캐시 갱신 시 needsUpload 상태 항목은 원격본으로 덮지 않는다 | LocalSampleRepositories.swift:86 | 일치 |
| SYNC-06 | 삭제는 로컬 tombstone 생성 후, localOnly면 즉시 물리 제거, 아니면 원격 삭제 성공 시에만 물리 제거하고 거부 시 복원한다 | LocalSampleRepositories.swift:72, OfflineFirstProjectRepository.swift:73 | 일치 |
| SYNC-07 | pending 재개는 조회 시점에만 일어난다. 앱 재실행 직후 자동 실행은 없다 | OfflineFirstProjectRepository.swift:17, :39 (유일 호출처) | 일치 |
| SYNC-08 | 프로젝트 저장 시 서버는 요청에 포함되지 않은 기존 자식 레코드를 무이력 삭제하지 않는다. 삭제가 필요한 경우 deletedAt 마킹을 따른다 | projects.service.ts:1424, :1457, :1520 (기존 자식 하드 deleteMany 후 재삽입) | 불일치 의심. 인수인계 문서 known issue #14+#16의 결합 경로를 QA 준비 단계에서 신규 특정. 부속 사실: 전체 PATCH를 타는 조작은 폼 수정, 도안 연결/해제, 드로잉 저장/삭제, 메모 저장, 완성 처리(ViewModel:861~901)이며 카운터만 개별 API를 쓴다. 도안 연결 해제는 하드 삭제에 파일 고아화까지 동반한다(PAT-04) |
| SYNC-09 | 업로드가 거부된 localOnly 항목은 로컬에 보존되고 실패 사유가 안내된다. 어떤 서버 응답도 서버에 존재한 적 없는 로컬 데이터의 삭제를 유발하지 않는다 | OfflineFirstProjectRepository.swift:230 (거부 시 물리 제거) | 불일치 의심. QA 준비 단계 신규 발견. 06 재현: Charles로 POST 4xx 주입 후 로컬 파일 확인 |
| SYNC-10 | 업로드가 거부된 수정분은 폐기하더라도 거부 사실과 사유를 안내한다. 무통보 폐기는 금지한다. 서버본 복원 자체는 허용한다 | OfflineFirstProjectRepository.swift:233 (무통보 덮어씀) | 불일치 의심. SYNC-09는 보존 요구, SYNC-10은 통보 요구로 수준이 다르다 |
| SYNC-11 | MVP에서 last-write-wins를 허용한다. 단 (1) 덮어쓰기가 자식 레코드의 무이력 삭제를 유발하지 않고(SYNC-08과 동일 요구) (2) 충돌 감지 도입을 전제로 한 한시 허용이다 | projects.service.ts update 무조건 덮어씀, 인수인계 문서 known issue #14 | 조건부 일치 |

#### SPEC-LINK 프로젝트-창고 연결

| ID | 동작 정의 | 근거 | 코드 일치 여부 |
| --- | --- | --- | --- |
| LINK-01 | 도안은 프로젝트당 복사본 1개다. 기존 도안이 있는 상태에서 새 도안을 연결하면 교체 확인창을 표시하고, 기존 도안의 드로잉이 삭제된다는 점을 안내한다. `취소` 시 기존 도안과 드로잉을 그대로 유지하고, `교체` 확정 시에만 새 도안으로 교체하며 기존 도안 복사본에 종속된 로컬 드로잉 상태를 비운다 | 제품 의도 확정. 현재 구현 근거: schema.prisma projectId @unique, ProjectWorkspaceViewModel.swift:906, :920 | 불일치 의심. 현재 코드 근거상 확인 없이 교체하는 것으로 보여 06 실행에서 확인창, 취소 보존, 확정 후 드로잉 초기화를 검증한다 |
| LINK-02 | ~~실 연결은 두 층위다. 대표 실은 프로젝트당 1개 참조(yarnId)에 스냅샷 동반이고, 사용 기록(ProjectYarnUsage)은 다중이되 대표 실에 대해서만 기록할 수 있다. 바늘은 프로젝트당 1개 참조에 스냅샷, 도구는 다중 연결이다~~ (v1.4 개정) 실과 바늘은 프로젝트에 여러 개를 연결할 수 있다. 사용 기록(ProjectYarnUsage)은 연결된 실 각각에 대해 기록할 수 있다. 각 연결은 연결 시점의 정보를 스냅샷으로 저장한다(LINK-05와 동일 원칙). 도구는 다중 연결로 현행을 유지한다. 요약 화면의 다중 실 표시 방식은 미정이다 | KnittingProject.swift:22, ViewModel:359(guard yarnId), :364, ProjectYarnUsage.swift:15 | 불일치 확정(기능 부재). 현재 구조는 실과 바늘이 프로젝트당 단일 참조이고 사용 기록은 대표 실 전제(guard yarnId)다. QA 실행 중(8/4) 기획 재검토로 기준을 개정했고, 개정 전 기준으로는 일치였다 |
| LINK-03 | 삭제된 스킬 ID는 스킬 조회와의 교집합에서 자동 제외되어 표시되지 않는다 | ViewModel:238, :256 | 일치. 경계 TC: 스킬 fetch 실패 시 에러 안내로 구분되는지 |
| LINK-04 | 창고에서 실 삭제 시 tombstone 처리되어 조회에서 제외되고, 프로젝트의 참조 ID와 연결 시점 스냅샷은 남는다. 프로젝트의 모든 화면은 원본 삭제 전후 동일한 스냅샷 값을 계속 표시한다 | LocalSampleRepositories.swift:748(deletedAt 필터), :762, ViewModel:270 | 조건부 일치. 원본 삭제 후 작업 화면이 스냅샷으로 전환되는 동작은 일치하나, 삭제 전에도 스냅샷을 표시해야 한다는 기준은 LINK-05에서 불일치 의심으로 관리 |
| LINK-05 | 실과 바늘은 프로젝트 연결 시점의 정보를 스냅샷으로 저장하며, 프로젝트의 작업 화면, 카드, 사용 기록 등 모든 화면은 원본의 생존, 수정, 삭제 여부와 관계없이 해당 스냅샷을 동일하게 표시한다. 창고 원본 수정은 기존 프로젝트에 전파하지 않으며, 수정 이후 새로 연결한 프로젝트에만 변경된 값이 저장된다 | ViewModel:281(원본 최신값) vs ProjectCardView:164, ProjectYarnUsagePanelView:15(스냅샷) | 불일치 의심. 현재 작업 화면은 원본 최신값을, 카드와 사용 기록 패널은 연결 시점 스냅샷을 표시하여 화면별 기준이 다르다. 새 기준에서는 작업 화면도 스냅샷을 표시해야 한다 |
| LINK-06 | 프로젝트 생성, 수정 폼에서 실, 바늘, 도구를 즉석 생성할 수 있다. 생성된 항목은 창고에 저장되고 해당 프로젝트에 자동 연결된다. 창고가 비어 있어도 재료 섹션은 표시된다 | 제품 의도 확정(2차 사이클). 1차 관찰 2번(즉석 생성 불가, 창고 선행 필수)의 불일치를 해소하는 기준. 구현 커밋 1471680, 369eb24 | 미대조. 2차 사이클 신규 TC에서 검증 |
| LINK-07 | 실과 바늘은 대표 1개와 추가 N개 구조로 연결한다. 추가 연결은 연결 시점 스냅샷을 포함하며(LINK-05와 동일 원칙), 정보 탭에서 연결, 해제와 실별 사용량 기록이 가능하다. 요약은 대표 외 N개로 표기한다. 연결과 해제는 오프라인에서도 가능하며 온라인 복귀 시 동기화된다 | 제품 의도 확정(2차 사이클). LINK-02 v1.4의 구현 형태. 구현 커밋 bd433e9 | 전단(대표+추가 구조, 스냅샷, 사용량 기록)은 미대조. 후단(오프라인 연결)은 불일치 확정(기능 부재). 현재 구현은 새 오프라인 캐시 표면을 만들지 않기 위해 온라인 필수로 두었으나, 기획 기준은 오프라인 지원이다. 확장 시 DEF-11(캐시 제거 부재) 계열 재발 위험을 함께 검토한다 |
| LINK-08 | 프로젝트 추가 폼에서 도구를 다중 선택해 연결할 수 있고, 프로젝트 저장과 함께 반영된다 | 제품 의도 확정(2차 사이클). 1차 관찰 7번(생성 시점 도구 연결 불가) 해소. 구현 커밋 269efca | 미대조. 2차 사이클 신규 TC에서 검증 |
| LINK-09 | 작업 화면의 수정과 목록의 수정은 동일한 폼과 데이터를 제공한다. 창고 데이터가 폼에 전달되어 진입 경로와 무관하게 재료 섹션이 표시된다 | 제품 의도 확정(2차 사이클). 1차 관찰 3, 4번(진입점 불일치, 안내 막다른 길) 해소. 구현 커밋 369eb24 | 미대조. 2차 사이클 신규 TC에서 검증 |

#### SPEC-CNT 단수카운터

| ID | 동작 정의 | 근거 | 코드 일치 여부 |
| --- | --- | --- | --- |
| CNT-01 | 증가 탭 1회당 1 증가한다. 감소와 직접 입력은 하한 0이고 상한은 없다 | ViewModel:786, :795 | 일치 |
| CNT-02 | 값이 바뀔 때마다 즉시 저장하며 전체 PATCH가 아니라 개별 카운터 API를 쓴다. 동일 값이면 저장을 생략한다 | ViewModel:1229, :1265 | 일치 |
| CNT-03 | 서버에 RowCounter 없는 프로젝트는 해당 프로젝트만 격리 실패하고 목록은 정상 표시된다 | KnittingProject.swift:34(클라 필수값, 커스텀 디코딩 없음), schema.prisma(서버 옵셔널) | 불일치 의심. 코드상 배열 단위 디코딩이라 목록 전체 실패 우려. 06 psql 실측 대상 |

#### SPEC-PAT 도안 등록과 열람

| ID | 동작 정의 | 근거 | 코드 일치 여부 |
| --- | --- | --- | --- |
| PAT-01 | 등록 입구는 5개다(파일 직접, 파일 가져오기 + 창고 저장, 창고에서 선택, 스캔, 제목 직접 입력). 같은 입력이면 경로와 무관하게 복사본 메타데이터와 서버 레코드가 동일하다 | ViewModel:906, :932, :945, ProjectWorkspaceView:773 (입구 5개와 경로별 결과 형태까지 실측, 경로 간 동일성은 미대조) | 전단(입구 5개)은 일치. 후단(경로 동등성)은 미대조, 06 검증 대상. CUJ-1 경로 동등성 직결 |
| PAT-02 | 복사본 파일은 로컬에 캐시된 뒤 표시된다 | RemoteProjectRepository.swift:17 | 일치 |
| PAT-05 | 작업 화면에서 앱 내부의 다른 화면(스킬 상세, 사전·스킬 검색)이나 다른 탭으로 이동했다가 정상적인 UI 동작으로 복귀하면, 열어둔 도안과 이탈 전 페이지 번호를 유지한다. 확대율과 페이지 내 스크롤 위치는 보존 대상에서 제외한다. 앱 재실행 후 유지는 약속 범위 밖으로 유보한다 | 제품 의도 확정. 2026-08-23 오너 결정으로 탭 이동 복귀 포함 범위 확대. 경위: UI-51 실행에서 탭 복귀 시 1페이지 초기화 관찰 | 탭 이동 경로 불일치 확정(DEF-19). 스킬 검색 이탈 경로는 8/23 실측 PASS. 1차 수정 후 UI-51 회귀 |
| PAT-03 | 드로잉은 복사본에 종속된다. 저장은 드로잉 파일 업로드 후 프로젝트 전체 PATCH를 다시 타는 이중 경로다. 삭제도 동일하다 | ViewModel:861, :880, 라우트 POST/GET/DELETE pattern-copy/drawing | 일치. SYNC-08 노출면 포함 |
| PAT-04 | 연결 해제는 복사본만 제거하며 창고 원본을 삭제하지 않는다. 해제로 참조가 끊긴 서버 파일은 회수 가능해야 한다 | ViewModel:975 (전체 PATCH patternCopy: null 경로), projects.service.ts:1520 (하드 삭제, fileStorage 호출 부재), schema.prisma:228, :232 (키 문자열 직접 참조) | 전단은 일치. 후단은 불일치 의심(운영 리스크, 회수 불가형 스토리지 누수). 근본 원인이 SYNC-08과 동일(레코드 하드 삭제)하므로 개선 권고는 하나로 묶는다. 07 실증 |
| PAT-08 | 드로잉은 보기 모드에서도 표시된다. 보기 모드는 표시만 하고 편집을 차단하며, 편집은 그리기 모드에서만 가능하다 | 제품 의도 확정(2차 사이클). 경위: UI-48 실행 중 보기 모드 드로잉 미표시 관찰 | 불일치 확정(DEF-18). 보기 모드에서 드로잉 캔버스 미렌더링(ProjectPatternFocusView.swift:149, 그리기 모드 조건부). 1차 수정 후 UI-49 회귀 |
| PAT-06 | 도안은 제목만으로 생성할 수 있고 파일은 나중에 첨부한다. 파일이 이미 있는 도안에 재첨부하면 400으로 거부한다. 교체는 첨부와 다른 경로다 | patterns.service.ts:148-153 (`Pattern already has a file.`), patterns.controller.ts:73 | 일치 (8/30 실측: 제목만 생성 201, 첫 첨부 201, 재첨부 400) |
| PAT-07 | 오프라인 진입 시 진행 사진은 다른 데이터와 같은 규칙으로 처리된다. 사진만 다른 경로를 타지 않는다 | 미대조. 클라이언트 오프라인 경로라 API로는 절반만 보인다 | 미대조 |

#### PAT-01 경로별 결과 상태

| 경로 | 복사본 | 창고 원본 | 로컬 파일 | 원본 연결 |
| --- | --- | --- | --- | --- |
| 파일 직접 (창고 저장 안 함) | 생성 | 없음 | 있음 | 없음 |
| 파일 가져오기 + 창고 저장 | 생성 | 생성 | 있음 | 있음 |
| 창고에서 선택 | 생성 | 기존 | 있음 | 있음 |
| 스캔 (파일 경로와 동일 입구, 창고 저장 선택 가능) | 생성 | 선택에 따름 | 있음 | 선택에 따름 |
| 제목 직접 입력 | 생성 (파일 없음) | 없음 | 없음 | 없음 |

#### 파일 정리 경로 현황 (PAT-04 부속)

| 경로 | 파일 정리 |
| --- | --- |
| 창고 도안 삭제 | 있음 (patterns.service.ts:167) |
| 진행 사진 삭제 | 있음 (projects.service.ts:827) |
| 도안 파일 재업로드 | 있음, 이전 키 remove (:918) |
| 드로잉 개별 삭제 | 있음 (:1009) |
| 연결 해제 (patternCopy null 전체 PATCH) | 없음. deleteMany(:1520) 본문에 fileStorage 호출 부재. 레코드 하드 삭제로 키 참조가 소멸해 지연 정리도 불가능 |

#### SPEC-AUTH 인증

| ID | 동작 정의 | 근거 | 코드 일치 여부 |
| --- | --- | --- | --- |
| AUTH-01 | 토큰은 Keychain에 저장되고 만료는 AUTH_JWT_EXPIRES_IN_SECONDS 설정을 따르며 기본 30일이다 | access-token.service.ts:117 | 일치. 저장 위치, 만료 설정 검증은 화면에서 불가하여 07 API, 구현 검증으로 이관 |
| AUTH-02 | 401 수신 시 세션만 제거하고 로컬 데이터는 유지한다 | AppRepositoryContainer.swift:123 | 일치 |
| AUTH-03 | 로그아웃은 세션만 제거한다 | AuthAccountViewModel.swift:144 | 일치 |
| AUTH-04 | 계정 B 로그인 시 A의 로컬 데이터가 보이지 않는다 | LocalSampleRepositories.swift:172 (계정 무구분 단일 파일) | 불일치 의심 |
| AUTH-05 | 업로드는 데이터를 생성한 계정의 세션으로만 전송된다 | 같은 근거 | 불일치 의심 |
| AUTH-06 | 로컬 캐시 잔존은 계정별 저장 분리가 전제될 때만 허용한다 | 같은 근거 | 불일치 확정. 현 구조(계정 무구분 단일 파일)가 전제를 위배. 실행 없이 정적 테스트(코드 구조 분석)로 식별한 명세-코드 불일치 |
| AUTH-07 | 로그아웃 또는 토큰 만료로 세션이 종료되면 로그인 화면으로 전환한다. 로그인하지 않은 상태에서 이전 계정이나 다른 사용자의 데이터가 표시되지 않는다 | 제품 의도 확정(2차 사이클). 1차 명세 공백 승격. 관찰 근거: 로그아웃 후 에러 화면에 잔류하고 로그인 입구가 설정 내부에만 존재, 만료 후 dev-user 데이터 노출 | 불일치 확정. AUTH-06과 동일 구조(익목 영역 캐시)에서 파생. 완전 해소는 세션 생명주기와 저장 디렉토리 재설계 필요 |

#### SPEC-TIME 작업 시간 (2차 사이클 신설)

| ID | 동작 정의 | 근거 | 코드 일치 여부 |
| --- | --- | --- | --- |
| TIME-01 | 작업 시간 세션은 프로젝트 화면 진입 시 자동으로 시작할 수 있다. 단 기록으로 확정하기 전에 사용자 확인을 받거나, 최소 임계 시간(1분) 미만인 세션은 저장하지 않는다. 기록된 시간이 사용자가 실제 작업하지 않은 구간을 포함해서는 안 된다 | 제품 의도 확정(2차 사이클). 1차 명세 공백 승격. 관찰 근거: 화면 진입만으로 27분짜리 세션 1건 생성 실측. 01의 제품 보장(누른 만큼 정확히 센다)의 연장 | 불일치 의심. 현재 구현은 자동 시작, 자동 종료이며 임계 시간이나 사용자 확인이 없는 것으로 보임. 2차 사이클 TC에서 확정 |

#### SPEC-UI 입력 보조와 알림 (2차 사이클 신설)

| ID | 동작 정의 | 근거 | 코드 일치 여부 |
| --- | --- | --- | --- |
| UI-01 | 실, 바늘, 도구 폼은 필수 입력 항목을 표시한다. 바늘 사이즈는 표준값 퀵픽과 자유 입력을 함께 제공한다. 카운터 음수 입력은 0으로 보정하고 보정 사실을 안내한다 | 제품 의도 확정(2차 사이클). 1차 관찰 5, 12번과 개선 제안(음수 무통보 보정) 해소. 구현 커밋 31a8123 | 미대조. 2차 사이클 신규 TC에서 검증 |
| UI-02 | 작업 완료 알림(프로젝트 추가 등)은 일정 시간 후 자동으로 사라지며, 그동안 사용자 조작을 차단하지 않는다 | 제품 의도 확정(2차 사이클). 1차 명세 공백 승격. 관찰 근거: 생성 알림이 화면 갱신 전까지 유지 | 불일치 의심. 2차 사이클 TC에서 확정 |

#### SPEC-PROJ 프로젝트 입력 (2차 사이클 신설)

| ID | 동작 정의 | 근거 | 코드 일치 여부 |
| --- | --- | --- | --- |
| PROJ-01 | 프로젝트 이름은 공백 제거 후 1자 이상 30자 이하다. 폼은 글자 수 카운터(n/30)를 표시하고, 30자 도달 시 추가 입력을 무시하며 붙여넣기 초과분은 30자로 잘라 반영한다. 클라이언트 폼과 서버 검증 2층에 적용한다. 기존 저장 데이터에 30자 초과 없음(8/23 psql 실측)으로 마이그레이션은 불요하다 | 제품 의도 확정(2차 사이클). 발견 경위: 케이스 설계 중 코드 3층(폼 canSave, 서버 DTO, 스키마) 확인으로 길이 제한 부재 식별. 무제한 허용의 리스크(표시 붕괴, 동기화 비용, 악용, 연계 시스템)를 근거로 스펙화 | 불일치 확정(기능 부재). UI-17 FAIL 예상, 1차 수정에서 구현 후 회귀 |

#### 명세 공백

| # | 내용 | 처리 |
| --- | --- | --- |
| 1 | 토큰 만료 도래 시 사용자 안내 UX | 공백 유지. 06에서 짧은 만료 설정으로 AUTH-02 경로만 확인 |
| 2 | 도안 교체나 해제로 참조가 끊긴 서버 파일의 정리 | 판정 완료: 운영 리스크(회수 불가형 스토리지 누수)로 분류. 사용자 데이터 훼손이 아니므로 03의 치명도 축에는 올리지 않되, 근본 원인이 SYNC-08과 동일하므로 개선 권고는 하나로 묶는다. 07에서 psql과 server/storage 대조로 실증 |

#### 부록 1. SPEC과 CUJ 매핑 (RTM 상위 연결)

| 상위 (01) | 관련 SPEC |
| --- | --- |
| CUJ-1 (첫 사용, 경로 동등성) | PAT-01, PAT-02, SYNC-01~03, CNT-01~02 |
| CUJ-2 (재방문, 보존) | SYNC-04~05, SYNC-07, CNT-02, AUTH-02 (AUTH-01은 07 이관) |
| CUJ-3 (연결 생애주기) | LINK-01~05, PAT-04, SYNC-06 |
| CUJ-4 (맥락 보존) | CNT-02, PAT-02, PAT-05 |
| CUJ 외 악조건 검증 (치명도 1~3위 심층) | SYNC-08~11, CNT-03, AUTH-04~06 |

#### 부록 2. 참조된 known issues (QA 인수인계 문서 발췌)

| # | 내용 |
| --- | --- |
| #14 | 동시 수정 시 승자 규칙 없음 (last-write-wins) |
| #16 | 프로젝트 PATCH가 자식 레코드를 전체 교체(full-replace) 방식으로 처리 |

#### 문서 이력

| 버전 | 일자 | 변경 |
| --- | --- | --- |
| 1.0 | 2026-08-01 | 최초 작성. 코드 실측 기반 SPEC 26건, 불일치 의심 7건 식별, RTM 추적 체계 수립 |
| 1.1 | 2026-08-02 | 제목을 기능 동작 정의서(검증 기준선, Test Basis)로 변경. 문서 정체성 선언과 테스트 오라클 원칙 문구 추가. 근거의 기준 커밋 시점 명시 |
| 1.2 | 2026-08-03 | SPEC 건수 표기 정정(26→29), 불일치 의심 집계 기준 통일(8건) |
| 1.3 | 2026-08-04 | 05 설계 중 SPEC 4건 추가로 30건 확정(PAT-05 신설, LINK-01, 04, 05 제품 기준 강화). 명세 공백 3번 추가. AUTH-01 07 이관 표기 |
| 1.4 | 2026-08-04 | LINK-02 개정. 실과 바늘의 다중 연결과 실별 사용 기록을 기준으로 변경. 사유는 QA 실행 중 기획 재검토. 코드 일치는 불일치 확정(기능 부재)으로 변경 |
| 2.0 | 2026-08-10 | 2차 사이클 기준선 확장으로 40건 확정. 신규 기능 SPEC 6건(LINK-06~09, PAT-06, UI-01)과 명세 공백 승격 4건(TIME-01, PAT-07, AUTH-07, UI-02) 추가. TIME, UI 영역 신설. LINK-07 후단은 오프라인 지원을 기획 기준으로 두고 현재 구현을 불일치 확정으로 판정 |
| 2.1 | 2026-08-24 | PROJ 영역 신설(PROJ-01 이름 길이 제한), PAT-05 범위 확대(탭 이동 복귀 포함, DEF-19 근거), PAT-08 신설(보기 모드 드로잉 표시, DEF-18 근거). 전부 2차 케이스 설계 중 발견분의 스펙화. 42건 확정 |

---

> **이관 시 확인 필요 (Claude 조사 노트, 판단은 QA)**
> ~~한눈에 요약과 문서 이력은 PAT 8건을 집계하지만 본문 SPEC-PAT 표에는 6건만 있다.~~ 2026-08-30 해소. 이력 v2.0에 신설로 기록된 PAT-06과 명세 공백 승격 PAT-07의 행을 본문에 채웠다. 05에 TC-PAT06-01과 TC-PAT07-01이 이미 있었으므로 SPEC은 존재했고 행만 누락돼 있었다. 집계는 42가 맞다.
