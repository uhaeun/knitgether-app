# POM 화면 목록과 성공/실패 기준 - 2026-07-19

## 목적

이 문서는 XCUITest POM 자동화에서 어떤 화면을 검증 대상으로 삼고, 각 화면을 어떤 기준으로 PASS/FAIL 처리하는지 정의한다. 테스트를 많이 만드는 것보다 먼저 "무엇을 보면 정상이라고 판단할 수 있는지"를 고정하는 QA 산출물이다.

## 공통 판정 규칙

| 구분 | 기준 |
|---|---|
| PASS | 사용자의 액션 후 기대한 화면, 버튼, 문구, 저장 결과가 제한 시간 안에 보인다. |
| FAIL | 기대 요소가 제한 시간 안에 나타나지 않거나, 잘못된 오류 메시지/상태가 보인다. |
| ENV_ISSUE | 서버 미실행, 시뮬레이터 대상 없음, 네트워크 연결 문제처럼 앱 기능 판단과 분리해야 하는 환경 문제가 원인이다. |
| NEED_SPEC_CONFIRM | 정상 동작 기준이 제품 요구사항에 아직 명확하지 않다. |

실패 시에는 XCUITest가 자동으로 실패 화면 스크린샷을 남긴다. 수동 QA 증거는 `docs/qa/portfolio/evidence/`에 저장한다.

## 현재 자동화된 핵심 흐름

| 항목 | 내용 |
|---|---|
| 테스트 | `testCoreUserFlowRegistersAndSavesCommonRecords` |
| 자동화 범위 | 회원가입 -> 온보딩 -> 스킬 테스트 저장 -> 프로젝트 생성 -> 게이지 기록 저장 |
| 실행 기준 | 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | 신규 사용자가 첫 진입 후 주요 저장 기능을 최소 1회 성공적으로 사용할 수 있는지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testProjectPersistsAfterAppRelaunch` |
| 자동화 범위 | 회원가입 -> 온보딩 완료 -> 프로젝트 생성 -> 앱 종료/재실행 -> 프로젝트 목록 확인 |
| 실행 기준 | 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | 저장한 프로젝트가 앱 재실행 후에도 같은 계정 목록에 유지되는지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testWorkspaceRowCounterPersistsAfterAppRelaunch` |
| 자동화 범위 | 회원가입 -> 프로젝트 생성 -> 작업공간 진입 -> 다음 단 -> 앱 종료/재실행 -> 단수 확인 |
| 실행 기준 | 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | 작업공간 단수 변경이 저장되고 앱 재실행 후에도 유지되는지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testWorkspaceWorkSessionCanBeRecorded` |
| 자동화 범위 | 회원가입 -> 프로젝트 생성 -> 작업공간 진입 -> 10초 이상 작업시간 기록 -> 세션 내역 확인 |
| 실행 기준 | 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | 10초 이상 작업 세션이 저장되어 세션 내역으로 접근 가능한지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testWorkspaceWorkSessionCanBeDeletedAndStaysDeletedAfterRelaunch` |
| 자동화 범위 | 프로젝트 생성 -> 10초 이상 작업시간 기록 -> 세션 내역에서 삭제 -> 앱 종료/재실행 -> 세션 내역 비활성 확인 |
| 실행 기준 | 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | 삭제한 WorkSession이 화면과 저장소에서 제거되고 앱 재실행 후 부활하지 않는지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testServerOffWithCachedProjectShowsCachedProject` |
| 자동화 범위 | 서버 ON 회원가입/프로젝트 생성 -> 같은 cache로 서버 OFF URL 재실행 -> 프로젝트 목록 확인 |
| 실행 기준 | 첫 실행 시 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | API 모드에서 remote 조회 실패 시 local cache fallback으로 사용자가 기존 프로젝트를 볼 수 있는지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testServerOffPendingProjectSyncsAfterServerRecovers` |
| 자동화 범위 | 서버 ON 회원가입 -> 서버 OFF URL로 프로젝트 생성 -> 서버 ON URL 복구 -> 빈 cache 재조회 |
| 실행 기준 | 로컬 API 서버가 복구 단계에서 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | 서버 OFF 중 저장한 pending 프로젝트가 서버 복구 후 원격에 반영되는지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testServerOffWithoutProjectCacheShowsOfflineNotice` |
| 자동화 범위 | 서버 ON 회원가입/온보딩 -> 빈 cache 경로로 서버 OFF URL 재실행 -> 내 뜨개 오프라인 안내 확인 |
| 실행 기준 | 첫 실행 시 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | cache가 전혀 없는 상태에서 remote 조회가 실패하면 빈 목록으로 오인하지 않고 서버 연결 안내를 보여주는지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testAccountSwitchKeepsProjectCacheSeparatedByUser` |
| 자동화 범위 | A 계정 회원가입/온보딩 -> 프로젝트 생성 -> 로그아웃 -> 같은 cache root로 B 계정 회원가입/온보딩 -> A 프로젝트 미노출 확인 |
| 실행 기준 | 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | 로그인 계정이 바뀔 때 local cache scope가 분리되어 다른 계정의 프로젝트가 노출되지 않는지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testExistingAccountCanLogInAndReachMainTabs` |
| 자동화 범위 | 기존 계정 회원가입/온보딩 -> 로그아웃 -> 앱 재실행 -> 같은 email/password로 로그인 -> 메인 탭 진입 확인 |
| 실행 기준 | 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | 재방문 사용자가 기존 계정으로 다시 로그인해 앱 사용 화면까지 진입할 수 있는지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testWrongPasswordLoginShowsErrorAndDoesNotPersistSession` |
| 자동화 범위 | 기존 계정 회원가입/온보딩 -> 로그아웃 -> 앱 재실행 -> 틀린 비밀번호 로그인 -> 오류 메시지 확인 -> 앱 재실행 후 signed-out 상태 확인 |
| 실행 기준 | 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | 인증 실패가 사용자에게 명확히 표시되고 실패한 로그인 세션이 저장되지 않는지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testLogoutDoesNotShowProfileLoadError` |
| 자동화 범위 | 로그인/온보딩 -> 설정 계정 화면에서 로그아웃 -> 프로필 편집 진입 -> 프로필 로드 오류 미노출 확인 |
| 실행 기준 | 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | 로그아웃 상태를 오류로 오인해 사용자에게 `프로필을 불러오지 못했어요.`를 보여주지 않는지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testProjectCanBeEditedDeletedAndStayDeletedAfterRelaunch` |
| 자동화 범위 | 프로젝트 생성 -> 작업공간 수정 시트에서 이름 변경 -> 앱 재실행 유지 확인 -> 삭제 -> 앱 재실행 후 미노출 확인 |
| 실행 기준 | 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | 프로젝트 수정/삭제가 화면과 저장소에 모두 반영되고 재실행 후에도 상태가 유지되는지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testWorkspaceRowInstructionCanBeSavedEditedDeletedAndStayDeletedAfterRelaunch` |
| 자동화 범위 | 프로젝트 생성 -> 작업공간 행안내 모드 전환 -> 행안내 추가 -> 수정 -> 앱 재실행 유지 확인 -> 삭제 -> 앱 재실행 후 미노출 확인 |
| 실행 기준 | 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | 행안내 저장/수정/삭제가 화면과 저장소에 반영되고 재실행 후에도 상태가 유지되는지 확인한다. |

| 항목 | 내용 |
|---|---|
| 테스트 | `testGaugeTargetCanBeEditedDeletedAndStayDeletedAfterRelaunch` |
| 자동화 범위 | GaugeTarget 추가 -> 앱 재실행 유지 확인 -> 수정 -> 앱 재실행 유지 확인 -> 삭제 -> 앱 재실행 후 미노출 확인 |
| 실행 기준 | 로컬 API 서버가 `http://127.0.0.1:3000/api/v1`에서 응답해야 한다. |
| 목적 | 목표 게이지 저장/수정/삭제가 화면과 저장소에 반영되고 재실행 후에도 상태가 유지되는지 확인한다. |

## POM 화면 목록

| POM | 화면/역할 | 성공 기준 | 실패 기준 |
|---|---|---|---|
| `KnitGetherUITestCase` | 테스트 실행 공통 설정 | 앱이 foreground 상태로 실행되고, cache 디렉터리/cache root와 서버 URL을 시나리오별로 주입하며, 실패 시 스크린샷이 첨부된다. | 앱 실행 실패, 서버 URL/cache 주입 실패, 실패 증거 미수집. |
| `OnboardingPage` | 온보딩 시작/스킬 테스트 진입 | `다음`, `스킬 테스트 시작`, `시작` 버튼을 통해 메인 탭으로 이동한다. | 다음 단계 버튼이 없거나 메인 탭 진입 전 멈춘다. |
| `AuthPage` | 회원가입/로그인/로그아웃 | 회원가입 후 `회원가입이 완료됐어요.` 또는 로그인 상태가 보이고, 기존 계정 로그인 후 `로그인했어요.` 또는 로그인 상태가 보인다. 잘못된 비밀번호 로그인 시 `이메일 또는 비밀번호가 맞지 않아요.`가 보이고 세션이 저장되지 않는다. | 회원가입/로그인 버튼/입력칸이 없거나 제출 후 로그인 상태가 아니거나, 로그인 실패 후 세션이 저장된다. |
| `SkillTestPage` | 온보딩 스킬 테스트 | 스킬 선택 후 저장하면 온보딩의 스킬 테스트 단계로 돌아온다. | 403 `SYSTEM_SKILL_READ_ONLY`, 저장 실패 문구, 또는 이전 화면 복귀 실패. |
| `MainTabBarPage` | 메인 탭 | `내 뜨개` 탭이 보이고, `내 뜨개`, `도구`, `설정` 탭 이동이 가능하다. | 탭이 없거나 탭 이동 후 목표 화면을 찾지 못한다. |
| `MyKnittingPage` | 내 뜨개 목록 | 프로젝트 저장 후 성공 메시지 또는 생성한 프로젝트명이 보이고, 재실행 후에도 생성한 프로젝트명이 보인다. cache 없는 오프라인 상태에서는 서버 연결 안내가 보인다. | 저장 후 목록에 프로젝트가 없거나 재실행 후 프로젝트가 사라지거나, cache 없는 오프라인 상태가 빈 목록으로 보인다. |
| `SettingsPage` | 설정 | 계정 화면과 프로필 화면으로 이동할 수 있고, 로그아웃 직후 프로필 로드 오류가 보이지 않는다. | 설정 탭, 계정 버튼, 프로필 버튼을 찾지 못하거나 로그아웃 상태를 오류로 표시한다. |
| `ProjectFormPage` | 프로젝트 생성/수정/삭제 폼 | 프로젝트명을 입력/교체하고 저장 버튼을 누를 수 있으며, 수정 시트의 삭제 확인 alert를 처리한다. | 입력칸/저장/삭제 버튼이 없거나 저장·삭제 액션이 발생하지 않는다. |
| `WorkspacePage` | 프로젝트 작업공간 | 단수 증가 후 현재 단수가 바뀌고, 재실행 후에도 유지된다. 10초 이상 작업시간을 종료하면 세션 내역에 진입할 수 있고, WorkSession 삭제 후 재실행해도 삭제 상태가 유지된다. 프로젝트 수정 시트와 행안내 모드/폼을 사용할 수 있다. | 단수가 저장되지 않거나, 작업시간 종료 후 세션 내역 버튼이 활성화되지 않거나, WorkSession 삭제 후 부활하거나, 수정 화면/행안내 폼에 진입하지 못한다. |
| `ToolPage` | 도구 목록 | 게이지 계산기 진입 버튼을 찾고 이동한다. | 도구 카드 또는 진입 버튼을 찾지 못한다. |
| `GaugeCalculatorPage` | 게이지 계산/저장 | 필수 수치를 입력하고 `세탁 전 게이지를 저장했어요.` 문구가 보인다. GaugeTarget 추가/수정/삭제 후 재실행 유지 상태를 확인할 수 있고, 지나치게 큰 숫자 입력 시 앱이 crash하지 않는다. | 입력 불가, 저장 버튼 비활성, 저장 성공 문구 미노출, GaugeTarget 삭제 후 부활, 큰 숫자 입력 시 crash. |
| `UITestPage` | POM 공통 액션 | 버튼/입력칸을 기다리고, 키보드와 스크롤 상태를 처리한다. | 요소가 존재하지만 누를 수 없는 상태가 계속된다. |

## 화면별 추가 수동 QA 기준

| 화면 | 우선순위 | 수동 확인 기준 |
|---|---|---|
| 회원가입/로그인 | P0 | 신규 가입, 기존 계정 로그인 재진입, 잘못된 비밀번호, 로그아웃 후 프로필 오류 미노출은 자동화 완료. 중복 이메일과 서버 OFF 오류 메시지를 확인한다. |
| 온보딩 | P0 | 앱 재설치 후 첫 진입, 온보딩 완료 후 재실행 시 메인 진입을 확인한다. |
| 스킬 테스트 | P0 | 시스템 스킬 수정이 아니라 사용자 스킬 레벨 저장으로 처리되는지 확인한다. |
| 프로젝트 생성/수정/삭제 | P0 | 생성/수정/삭제 후 앱 재실행 유지는 자동화 완료. 서버 DB 상세 반영과 오프라인 후 재동기화는 추가 확인한다. |
| 작업공간 | P0 | RowCounter 저장, WorkSession 기록/삭제, RowInstruction 저장/수정/삭제는 자동화 완료. |
| 게이지 | P1 | GaugeRecord 저장과 GaugeTarget 생성/수정/삭제/앱 재실행 후 유지는 자동화 완료. 스와치/측정 상세 흐름은 수동 확인한다. |
| 계정 전환 | P1 | A 계정 프로젝트가 B 계정에서 보이지 않는지 확인한다. 자동화 완료. |
| 서버 OFF | P1 | cache fallback, pending 프로젝트 복구, cache 없음 오프라인 안내는 자동화 완료. |

## 다음 자동화 후보

| 후보 | 이유 | 예상 POM |
|---|---|---|
| 스와치/측정 상세 흐름 | 게이지 영역의 남은 수동 확인 범위 | `ToolPage`, `GaugeCalculatorPage` |

## 증거 기록 규칙

테스트 결과를 문서에 옮길 때는 아래 5가지를 같이 적는다.

| 항목 | 예시 |
|---|---|
| 앱 커밋 | `2fe2c82` |
| 실행 환경 | iPhone 16 Pro Simulator, iOS 18.5 |
| 서버 상태 | local API ON, test DB |
| 실행 명령 | `xcodebuild test ... -only-testing:KnitGetherUITests/...` |
| 결과 | PASS/FAIL/ENV_ISSUE + 실패 시 스크린샷 경로 |
